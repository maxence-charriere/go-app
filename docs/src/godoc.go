package main

// Code generated. DO NOT EDIT

import "github.com/maxence-charriere/go-app/v7/pkg/app"

// GodocMenu returns a go-app representation of the named documentation.
func GodocMenu() app.UI {
	return app.Div().
		Class("godoc-menu").
		ID("manual-nav").
		Body(
			app.Dl().Body(
				app.Dd().Body(
					app.A().
						Href("#pkg-variables").
						Body(
							app.Text("Variables"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#CopyBytesToGo").
						Body(
							app.Text("func CopyBytesToGo(dst []byte, src Value) int"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#CopyBytesToJS").
						Body(
							app.Text("func CopyBytesToJS(dst Value, src []byte) int"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dispatch").
						Body(
							app.Text("func Dispatch(f func())"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#GenerateStaticWebsite").
						Body(
							app.Text("func GenerateStaticWebsite(dir string, h *Handler) error"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Getenv").
						Body(
							app.Text("func Getenv(k string) string"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#KeepBodyClean").
						Body(
							app.Text("func KeepBodyClean() (close func())"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Log").
						Body(
							app.Text("func Log(format string, v ...interface{})"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Navigate").
						Body(
							app.Text("func Navigate(rawurl string)"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#NewContextMenu").
						Body(
							app.Text("func NewContextMenu(menuItems ...MenuItemNode)"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Reload").
						Body(
							app.Text("func Reload()"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Route").
						Body(
							app.Text("func Route(path string, node UI)"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#RouteWithRegexp").
						Body(
							app.Text("func RouteWithRegexp(pattern string, node UI)"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Run").
						Body(
							app.Text("func Run()"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#StaticResource").
						Body(
							app.Text("func StaticResource(path string) string"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#TestMatch").
						Body(
							app.Text("func TestMatch(tree UI, d TestUIDescriptor) error"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#TestPath").
						Body(
							app.Text("func TestPath(p ...int) []int"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#BrowserStorage").
						Body(
							app.Text("type BrowserStorage"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#BrowserWindow").
						Body(
							app.Text("type BrowserWindow"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Window").
						Body(
							app.Text("func Window() BrowserWindow"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Compo").
						Body(
							app.Text("type Compo"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Compo.JSValue").
						Body(
							app.Text("func (c *Compo) JSValue() Value"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Compo.Kind").
						Body(
							app.Text("func (c *Compo) Kind() Kind"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Compo.Mounted").
						Body(
							app.Text("func (c *Compo) Mounted() bool"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Compo.Render").
						Body(
							app.Text("func (c *Compo) Render() UI"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Compo.Update").
						Body(
							app.Text("func (c *Compo) Update()"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Composer").
						Body(
							app.Text("type Composer"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Condition").
						Body(
							app.Text("type Condition"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#If").
						Body(
							app.Text("func If(expr bool, elems ...UI) Condition"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Context").
						Body(
							app.Text("type Context"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dismounter").
						Body(
							app.Text("type Dismounter"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dispatcher").
						Body(
							app.Text("type Dispatcher"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Environment").
						Body(
							app.Text("type Environment"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Event").
						Body(
							app.Text("type Event"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Event.PreventDefault").
						Body(
							app.Text("func (e Event) PreventDefault()"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#EventHandler").
						Body(
							app.Text("type EventHandler"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Func").
						Body(
							app.Text("type Func"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#FuncOf").
						Body(
							app.Text("func FuncOf(fn func(this Value, args []Value) interface{}) Func"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLA").
						Body(
							app.Text("type HTMLA"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#A").
						Body(
							app.Text("func A() HTMLA"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLAbbr").
						Body(
							app.Text("type HTMLAbbr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Abbr").
						Body(
							app.Text("func Abbr() HTMLAbbr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLAddress").
						Body(
							app.Text("type HTMLAddress"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Address").
						Body(
							app.Text("func Address() HTMLAddress"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLArea").
						Body(
							app.Text("type HTMLArea"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Area").
						Body(
							app.Text("func Area() HTMLArea"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLArticle").
						Body(
							app.Text("type HTMLArticle"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Article").
						Body(
							app.Text("func Article() HTMLArticle"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLAside").
						Body(
							app.Text("type HTMLAside"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Aside").
						Body(
							app.Text("func Aside() HTMLAside"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLAudio").
						Body(
							app.Text("type HTMLAudio"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Audio").
						Body(
							app.Text("func Audio() HTMLAudio"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLB").
						Body(
							app.Text("type HTMLB"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#B").
						Body(
							app.Text("func B() HTMLB"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLBase").
						Body(
							app.Text("type HTMLBase"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Base").
						Body(
							app.Text("func Base() HTMLBase"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLBdi").
						Body(
							app.Text("type HTMLBdi"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Bdi").
						Body(
							app.Text("func Bdi() HTMLBdi"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLBdo").
						Body(
							app.Text("type HTMLBdo"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Bdo").
						Body(
							app.Text("func Bdo() HTMLBdo"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLBlockquote").
						Body(
							app.Text("type HTMLBlockquote"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Blockquote").
						Body(
							app.Text("func Blockquote() HTMLBlockquote"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLBody").
						Body(
							app.Text("type HTMLBody"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Body").
						Body(
							app.Text("func Body() HTMLBody"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLBr").
						Body(
							app.Text("type HTMLBr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Br").
						Body(
							app.Text("func Br() HTMLBr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLButton").
						Body(
							app.Text("type HTMLButton"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Button").
						Body(
							app.Text("func Button() HTMLButton"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLCanvas").
						Body(
							app.Text("type HTMLCanvas"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Canvas").
						Body(
							app.Text("func Canvas() HTMLCanvas"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLCaption").
						Body(
							app.Text("type HTMLCaption"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Caption").
						Body(
							app.Text("func Caption() HTMLCaption"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLCite").
						Body(
							app.Text("type HTMLCite"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Cite").
						Body(
							app.Text("func Cite() HTMLCite"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLCode").
						Body(
							app.Text("type HTMLCode"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Code").
						Body(
							app.Text("func Code() HTMLCode"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLCol").
						Body(
							app.Text("type HTMLCol"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Col").
						Body(
							app.Text("func Col() HTMLCol"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLColGroup").
						Body(
							app.Text("type HTMLColGroup"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#ColGroup").
						Body(
							app.Text("func ColGroup() HTMLColGroup"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLData").
						Body(
							app.Text("type HTMLData"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Data").
						Body(
							app.Text("func Data() HTMLData"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDataList").
						Body(
							app.Text("type HTMLDataList"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#DataList").
						Body(
							app.Text("func DataList() HTMLDataList"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDd").
						Body(
							app.Text("type HTMLDd"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dd").
						Body(
							app.Text("func Dd() HTMLDd"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDel").
						Body(
							app.Text("type HTMLDel"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Del").
						Body(
							app.Text("func Del() HTMLDel"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDetails").
						Body(
							app.Text("type HTMLDetails"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Details").
						Body(
							app.Text("func Details() HTMLDetails"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDfn").
						Body(
							app.Text("type HTMLDfn"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dfn").
						Body(
							app.Text("func Dfn() HTMLDfn"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDialog").
						Body(
							app.Text("type HTMLDialog"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dialog").
						Body(
							app.Text("func Dialog() HTMLDialog"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDiv").
						Body(
							app.Text("type HTMLDiv"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Div").
						Body(
							app.Text("func Div() HTMLDiv"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDl").
						Body(
							app.Text("type HTMLDl"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dl").
						Body(
							app.Text("func Dl() HTMLDl"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLDt").
						Body(
							app.Text("type HTMLDt"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Dt").
						Body(
							app.Text("func Dt() HTMLDt"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLEm").
						Body(
							app.Text("type HTMLEm"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Em").
						Body(
							app.Text("func Em() HTMLEm"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLEmbed").
						Body(
							app.Text("type HTMLEmbed"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Embed").
						Body(
							app.Text("func Embed() HTMLEmbed"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLFieldSet").
						Body(
							app.Text("type HTMLFieldSet"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#FieldSet").
						Body(
							app.Text("func FieldSet() HTMLFieldSet"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLFigCaption").
						Body(
							app.Text("type HTMLFigCaption"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#FigCaption").
						Body(
							app.Text("func FigCaption() HTMLFigCaption"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLFigure").
						Body(
							app.Text("type HTMLFigure"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Figure").
						Body(
							app.Text("func Figure() HTMLFigure"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLFooter").
						Body(
							app.Text("type HTMLFooter"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Footer").
						Body(
							app.Text("func Footer() HTMLFooter"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLForm").
						Body(
							app.Text("type HTMLForm"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Form").
						Body(
							app.Text("func Form() HTMLForm"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLH1").
						Body(
							app.Text("type HTMLH1"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#H1").
						Body(
							app.Text("func H1() HTMLH1"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLH2").
						Body(
							app.Text("type HTMLH2"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#H2").
						Body(
							app.Text("func H2() HTMLH2"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLH3").
						Body(
							app.Text("type HTMLH3"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#H3").
						Body(
							app.Text("func H3() HTMLH3"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLH4").
						Body(
							app.Text("type HTMLH4"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#H4").
						Body(
							app.Text("func H4() HTMLH4"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLH5").
						Body(
							app.Text("type HTMLH5"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#H5").
						Body(
							app.Text("func H5() HTMLH5"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLH6").
						Body(
							app.Text("type HTMLH6"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#H6").
						Body(
							app.Text("func H6() HTMLH6"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLHead").
						Body(
							app.Text("type HTMLHead"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Head").
						Body(
							app.Text("func Head() HTMLHead"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLHeader").
						Body(
							app.Text("type HTMLHeader"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Header").
						Body(
							app.Text("func Header() HTMLHeader"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLHr").
						Body(
							app.Text("type HTMLHr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Hr").
						Body(
							app.Text("func Hr() HTMLHr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLHtml").
						Body(
							app.Text("type HTMLHtml"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Html").
						Body(
							app.Text("func Html() HTMLHtml"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLI").
						Body(
							app.Text("type HTMLI"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#I").
						Body(
							app.Text("func I() HTMLI"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLIFrame").
						Body(
							app.Text("type HTMLIFrame"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#IFrame").
						Body(
							app.Text("func IFrame() HTMLIFrame"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLImg").
						Body(
							app.Text("type HTMLImg"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Img").
						Body(
							app.Text("func Img() HTMLImg"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLInput").
						Body(
							app.Text("type HTMLInput"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Input").
						Body(
							app.Text("func Input() HTMLInput"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLIns").
						Body(
							app.Text("type HTMLIns"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Ins").
						Body(
							app.Text("func Ins() HTMLIns"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLKbd").
						Body(
							app.Text("type HTMLKbd"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Kbd").
						Body(
							app.Text("func Kbd() HTMLKbd"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLLabel").
						Body(
							app.Text("type HTMLLabel"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Label").
						Body(
							app.Text("func Label() HTMLLabel"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLLegends").
						Body(
							app.Text("type HTMLLegends"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Legends").
						Body(
							app.Text("func Legends() HTMLLegends"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLLi").
						Body(
							app.Text("type HTMLLi"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Li").
						Body(
							app.Text("func Li() HTMLLi"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLLink").
						Body(
							app.Text("type HTMLLink"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Link").
						Body(
							app.Text("func Link() HTMLLink"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLMain").
						Body(
							app.Text("type HTMLMain"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Main").
						Body(
							app.Text("func Main() HTMLMain"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLMap").
						Body(
							app.Text("type HTMLMap"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Map").
						Body(
							app.Text("func Map() HTMLMap"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLMark").
						Body(
							app.Text("type HTMLMark"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Mark").
						Body(
							app.Text("func Mark() HTMLMark"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLMeta").
						Body(
							app.Text("type HTMLMeta"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Meta").
						Body(
							app.Text("func Meta() HTMLMeta"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLMeter").
						Body(
							app.Text("type HTMLMeter"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Meter").
						Body(
							app.Text("func Meter() HTMLMeter"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLNav").
						Body(
							app.Text("type HTMLNav"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Nav").
						Body(
							app.Text("func Nav() HTMLNav"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLNoScript").
						Body(
							app.Text("type HTMLNoScript"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#NoScript").
						Body(
							app.Text("func NoScript() HTMLNoScript"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLObject").
						Body(
							app.Text("type HTMLObject"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Object").
						Body(
							app.Text("func Object() HTMLObject"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLOl").
						Body(
							app.Text("type HTMLOl"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Ol").
						Body(
							app.Text("func Ol() HTMLOl"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLOptGroup").
						Body(
							app.Text("type HTMLOptGroup"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#OptGroup").
						Body(
							app.Text("func OptGroup() HTMLOptGroup"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLOption").
						Body(
							app.Text("type HTMLOption"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Option").
						Body(
							app.Text("func Option() HTMLOption"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLOutput").
						Body(
							app.Text("type HTMLOutput"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Output").
						Body(
							app.Text("func Output() HTMLOutput"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLP").
						Body(
							app.Text("type HTMLP"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#P").
						Body(
							app.Text("func P() HTMLP"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLParam").
						Body(
							app.Text("type HTMLParam"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Param").
						Body(
							app.Text("func Param() HTMLParam"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLPicture").
						Body(
							app.Text("type HTMLPicture"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Picture").
						Body(
							app.Text("func Picture() HTMLPicture"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLPre").
						Body(
							app.Text("type HTMLPre"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Pre").
						Body(
							app.Text("func Pre() HTMLPre"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLProgress").
						Body(
							app.Text("type HTMLProgress"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Progress").
						Body(
							app.Text("func Progress() HTMLProgress"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLQ").
						Body(
							app.Text("type HTMLQ"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Q").
						Body(
							app.Text("func Q() HTMLQ"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLRp").
						Body(
							app.Text("type HTMLRp"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Rp").
						Body(
							app.Text("func Rp() HTMLRp"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLRt").
						Body(
							app.Text("type HTMLRt"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Rt").
						Body(
							app.Text("func Rt() HTMLRt"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLRuby").
						Body(
							app.Text("type HTMLRuby"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Ruby").
						Body(
							app.Text("func Ruby() HTMLRuby"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLS").
						Body(
							app.Text("type HTMLS"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#S").
						Body(
							app.Text("func S() HTMLS"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSamp").
						Body(
							app.Text("type HTMLSamp"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Samp").
						Body(
							app.Text("func Samp() HTMLSamp"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLScript").
						Body(
							app.Text("type HTMLScript"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Script").
						Body(
							app.Text("func Script() HTMLScript"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSection").
						Body(
							app.Text("type HTMLSection"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Section").
						Body(
							app.Text("func Section() HTMLSection"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSelect").
						Body(
							app.Text("type HTMLSelect"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Select").
						Body(
							app.Text("func Select() HTMLSelect"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSmall").
						Body(
							app.Text("type HTMLSmall"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Small").
						Body(
							app.Text("func Small() HTMLSmall"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSource").
						Body(
							app.Text("type HTMLSource"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Source").
						Body(
							app.Text("func Source() HTMLSource"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSpan").
						Body(
							app.Text("type HTMLSpan"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Span").
						Body(
							app.Text("func Span() HTMLSpan"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLStrong").
						Body(
							app.Text("type HTMLStrong"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Strong").
						Body(
							app.Text("func Strong() HTMLStrong"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLStyle").
						Body(
							app.Text("type HTMLStyle"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Style").
						Body(
							app.Text("func Style() HTMLStyle"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSub").
						Body(
							app.Text("type HTMLSub"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Sub").
						Body(
							app.Text("func Sub() HTMLSub"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSummary").
						Body(
							app.Text("type HTMLSummary"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Summary").
						Body(
							app.Text("func Summary() HTMLSummary"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLSup").
						Body(
							app.Text("type HTMLSup"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Sup").
						Body(
							app.Text("func Sup() HTMLSup"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTBody").
						Body(
							app.Text("type HTMLTBody"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#TBody").
						Body(
							app.Text("func TBody() HTMLTBody"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTHead").
						Body(
							app.Text("type HTMLTHead"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#THead").
						Body(
							app.Text("func THead() HTMLTHead"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTable").
						Body(
							app.Text("type HTMLTable"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Table").
						Body(
							app.Text("func Table() HTMLTable"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTd").
						Body(
							app.Text("type HTMLTd"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Td").
						Body(
							app.Text("func Td() HTMLTd"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTemplate").
						Body(
							app.Text("type HTMLTemplate"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Template").
						Body(
							app.Text("func Template() HTMLTemplate"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTextarea").
						Body(
							app.Text("type HTMLTextarea"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Textarea").
						Body(
							app.Text("func Textarea() HTMLTextarea"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTfoot").
						Body(
							app.Text("type HTMLTfoot"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Tfoot").
						Body(
							app.Text("func Tfoot() HTMLTfoot"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTh").
						Body(
							app.Text("type HTMLTh"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Th").
						Body(
							app.Text("func Th() HTMLTh"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTime").
						Body(
							app.Text("type HTMLTime"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Time").
						Body(
							app.Text("func Time() HTMLTime"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTitle").
						Body(
							app.Text("type HTMLTitle"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Title").
						Body(
							app.Text("func Title() HTMLTitle"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLTr").
						Body(
							app.Text("type HTMLTr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Tr").
						Body(
							app.Text("func Tr() HTMLTr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLU").
						Body(
							app.Text("type HTMLU"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#U").
						Body(
							app.Text("func U() HTMLU"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLUl").
						Body(
							app.Text("type HTMLUl"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Ul").
						Body(
							app.Text("func Ul() HTMLUl"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLVar").
						Body(
							app.Text("type HTMLVar"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Var").
						Body(
							app.Text("func Var() HTMLVar"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLVideo").
						Body(
							app.Text("type HTMLVideo"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Video").
						Body(
							app.Text("func Video() HTMLVideo"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#HTMLWbr").
						Body(
							app.Text("type HTMLWbr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Wbr").
						Body(
							app.Text("func Wbr() HTMLWbr"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Handler").
						Body(
							app.Text("type Handler"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Handler.ServeHTTP").
						Body(
							app.Text("func (h *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request)"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Icon").
						Body(
							app.Text("type Icon"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Kind").
						Body(
							app.Text("type Kind"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Kind.String").
						Body(
							app.Text("func (k Kind) String() string"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#MenuItemNode").
						Body(
							app.Text("type MenuItemNode"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#MenuItem").
						Body(
							app.Text("func MenuItem() MenuItemNode"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Mounter").
						Body(
							app.Text("type Mounter"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Navigator").
						Body(
							app.Text("type Navigator"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#RangeLoop").
						Body(
							app.Text("type RangeLoop"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Range").
						Body(
							app.Text("func Range(src interface{}) RangeLoop"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#ResourceProvider").
						Body(
							app.Text("type ResourceProvider"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#GitHubPages").
						Body(
							app.Text("func GitHubPages(repoName string) ResourceProvider"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#LocalDir").
						Body(
							app.Text("func LocalDir(path string) ResourceProvider"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#RemoteBucket").
						Body(
							app.Text("func RemoteBucket(url string) ResourceProvider"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#TestUIDescriptor").
						Body(
							app.Text("type TestUIDescriptor"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Type").
						Body(
							app.Text("type Type"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#UI").
						Body(
							app.Text("type UI"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#FilterUIElems").
						Body(
							app.Text("func FilterUIElems(uis ...UI) []UI"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Raw").
						Body(
							app.Text("func Raw(v string) UI"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Text").
						Body(
							app.Text("func Text(v interface{}) UI"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#UIShell").
						Body(
							app.Text("type UIShell"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Shell").
						Body(
							app.Text("func Shell() UIShell"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Value").
						Body(
							app.Text("type Value"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Null").
						Body(
							app.Text("func Null() Value"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Undefined").
						Body(
							app.Text("func Undefined() Value"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#ValueOf").
						Body(
							app.Text("func ValueOf(x interface{}) Value"),
						),
				),
				app.Dd().Body(
					app.A().
						Href("#Wrapper").
						Body(
							app.Text("type Wrapper"),
						),
				),
			),
		)
}

// Godoc returns a go-app representation of the named documentation.
func Godoc() app.UI {
	return app.Div().
		Class("godoc").
		ID("page").
		Body(
			app.Div().
				Class("container").
				Body(
					app.H1().Body(
						app.Text("Package app"),
						app.Span().
							Class("text-muted").
							Body(),
					),
					app.Div().
						ID("nav").
						Body(),
					app.Script().Body(
						app.Text("document.ANALYSIS_DATA = null;\n\tdocument.CALLGRAPH = null;"),
					),
					app.Div().
						ID("short-nav").
						Body(
							app.Dl().Body(
								app.Dd().Body(
									app.Code().Body(
										app.Text("import \"github.com/maxence-charriere/go-app/v7/pkg/app\""),
									),
								),
							),
							app.Dl().Body(
								app.Dd().Body(
									app.A().
										Href("#pkg-overview").
										Class("overviewLink").
										Body(
											app.Text("Overview"),
										),
								),
								app.Dd().Body(
									app.A().
										Href("#pkg-index").
										Class("indexLink").
										Body(
											app.Text("Index"),
										),
								),
								app.Dd().Body(
									app.A().
										Href("#pkg-subdirectories").
										Body(
											app.Text("Subdirectories"),
										),
								),
							),
						),
					app.Div().
						ID("pkg-overview").
						Class("toggleVisible").
						Body(
							app.Div().
								Class("collapsed").
								Body(
									app.H2().
										Class("toggleButton").
										Title("Click to show Overview section").
										Body(
											app.Text("Overview ▹"),
										),
								),
							app.Div().
								Class("expanded").
								Body(
									app.H2().
										Class("toggleButton").
										Title("Click to hide Overview section").
										Body(
											app.Text("Overview ▾"),
										),
								),
						),
					app.Div().
						ID("pkg-index").
						Class("toggleVisible").
						Body(
							app.Div().
								Class("collapsed").
								Body(
									app.H2().
										Class("toggleButton").
										Title("Click to show Index section").
										Body(
											app.Text("Index ▹"),
										),
								),
							app.Div().
								Class("expanded").
								Body(
									app.H2().
										Class("toggleButton").
										Title("Click to hide Index section").
										Body(
											app.Text("Index ▾"),
										),
									app.Div().
										ID("manual-nav").
										Body(
											app.Dl().Body(
												app.Dd().Body(
													app.A().
														Href("#pkg-variables").
														Body(
															app.Text("Variables"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#CopyBytesToGo").
														Body(
															app.Text("func CopyBytesToGo(dst []byte, src Value) int"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#CopyBytesToJS").
														Body(
															app.Text("func CopyBytesToJS(dst Value, src []byte) int"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dispatch").
														Body(
															app.Text("func Dispatch(f func())"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#GenerateStaticWebsite").
														Body(
															app.Text("func GenerateStaticWebsite(dir string, h *Handler) error"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Getenv").
														Body(
															app.Text("func Getenv(k string) string"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#KeepBodyClean").
														Body(
															app.Text("func KeepBodyClean() (close func())"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Log").
														Body(
															app.Text("func Log(format string, v ...interface{})"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Navigate").
														Body(
															app.Text("func Navigate(rawurl string)"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#NewContextMenu").
														Body(
															app.Text("func NewContextMenu(menuItems ...MenuItemNode)"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Reload").
														Body(
															app.Text("func Reload()"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Route").
														Body(
															app.Text("func Route(path string, node UI)"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#RouteWithRegexp").
														Body(
															app.Text("func RouteWithRegexp(pattern string, node UI)"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Run").
														Body(
															app.Text("func Run()"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#StaticResource").
														Body(
															app.Text("func StaticResource(path string) string"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#TestMatch").
														Body(
															app.Text("func TestMatch(tree UI, d TestUIDescriptor) error"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#TestPath").
														Body(
															app.Text("func TestPath(p ...int) []int"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#BrowserStorage").
														Body(
															app.Text("type BrowserStorage"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#BrowserWindow").
														Body(
															app.Text("type BrowserWindow"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Window").
														Body(
															app.Text("func Window() BrowserWindow"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Compo").
														Body(
															app.Text("type Compo"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Compo.JSValue").
														Body(
															app.Text("func (c *Compo) JSValue() Value"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Compo.Kind").
														Body(
															app.Text("func (c *Compo) Kind() Kind"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Compo.Mounted").
														Body(
															app.Text("func (c *Compo) Mounted() bool"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Compo.Render").
														Body(
															app.Text("func (c *Compo) Render() UI"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Compo.Update").
														Body(
															app.Text("func (c *Compo) Update()"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Composer").
														Body(
															app.Text("type Composer"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Condition").
														Body(
															app.Text("type Condition"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#If").
														Body(
															app.Text("func If(expr bool, elems ...UI) Condition"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Context").
														Body(
															app.Text("type Context"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dismounter").
														Body(
															app.Text("type Dismounter"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dispatcher").
														Body(
															app.Text("type Dispatcher"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Environment").
														Body(
															app.Text("type Environment"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Event").
														Body(
															app.Text("type Event"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Event.PreventDefault").
														Body(
															app.Text("func (e Event) PreventDefault()"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#EventHandler").
														Body(
															app.Text("type EventHandler"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Func").
														Body(
															app.Text("type Func"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#FuncOf").
														Body(
															app.Text("func FuncOf(fn func(this Value, args []Value) interface{}) Func"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLA").
														Body(
															app.Text("type HTMLA"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#A").
														Body(
															app.Text("func A() HTMLA"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLAbbr").
														Body(
															app.Text("type HTMLAbbr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Abbr").
														Body(
															app.Text("func Abbr() HTMLAbbr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLAddress").
														Body(
															app.Text("type HTMLAddress"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Address").
														Body(
															app.Text("func Address() HTMLAddress"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLArea").
														Body(
															app.Text("type HTMLArea"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Area").
														Body(
															app.Text("func Area() HTMLArea"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLArticle").
														Body(
															app.Text("type HTMLArticle"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Article").
														Body(
															app.Text("func Article() HTMLArticle"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLAside").
														Body(
															app.Text("type HTMLAside"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Aside").
														Body(
															app.Text("func Aside() HTMLAside"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLAudio").
														Body(
															app.Text("type HTMLAudio"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Audio").
														Body(
															app.Text("func Audio() HTMLAudio"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLB").
														Body(
															app.Text("type HTMLB"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#B").
														Body(
															app.Text("func B() HTMLB"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLBase").
														Body(
															app.Text("type HTMLBase"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Base").
														Body(
															app.Text("func Base() HTMLBase"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLBdi").
														Body(
															app.Text("type HTMLBdi"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Bdi").
														Body(
															app.Text("func Bdi() HTMLBdi"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLBdo").
														Body(
															app.Text("type HTMLBdo"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Bdo").
														Body(
															app.Text("func Bdo() HTMLBdo"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLBlockquote").
														Body(
															app.Text("type HTMLBlockquote"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Blockquote").
														Body(
															app.Text("func Blockquote() HTMLBlockquote"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLBody").
														Body(
															app.Text("type HTMLBody"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Body").
														Body(
															app.Text("func Body() HTMLBody"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLBr").
														Body(
															app.Text("type HTMLBr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Br").
														Body(
															app.Text("func Br() HTMLBr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLButton").
														Body(
															app.Text("type HTMLButton"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Button").
														Body(
															app.Text("func Button() HTMLButton"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLCanvas").
														Body(
															app.Text("type HTMLCanvas"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Canvas").
														Body(
															app.Text("func Canvas() HTMLCanvas"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLCaption").
														Body(
															app.Text("type HTMLCaption"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Caption").
														Body(
															app.Text("func Caption() HTMLCaption"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLCite").
														Body(
															app.Text("type HTMLCite"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Cite").
														Body(
															app.Text("func Cite() HTMLCite"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLCode").
														Body(
															app.Text("type HTMLCode"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Code").
														Body(
															app.Text("func Code() HTMLCode"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLCol").
														Body(
															app.Text("type HTMLCol"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Col").
														Body(
															app.Text("func Col() HTMLCol"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLColGroup").
														Body(
															app.Text("type HTMLColGroup"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#ColGroup").
														Body(
															app.Text("func ColGroup() HTMLColGroup"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLData").
														Body(
															app.Text("type HTMLData"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Data").
														Body(
															app.Text("func Data() HTMLData"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDataList").
														Body(
															app.Text("type HTMLDataList"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#DataList").
														Body(
															app.Text("func DataList() HTMLDataList"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDd").
														Body(
															app.Text("type HTMLDd"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dd").
														Body(
															app.Text("func Dd() HTMLDd"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDel").
														Body(
															app.Text("type HTMLDel"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Del").
														Body(
															app.Text("func Del() HTMLDel"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDetails").
														Body(
															app.Text("type HTMLDetails"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Details").
														Body(
															app.Text("func Details() HTMLDetails"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDfn").
														Body(
															app.Text("type HTMLDfn"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dfn").
														Body(
															app.Text("func Dfn() HTMLDfn"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDialog").
														Body(
															app.Text("type HTMLDialog"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dialog").
														Body(
															app.Text("func Dialog() HTMLDialog"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDiv").
														Body(
															app.Text("type HTMLDiv"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Div").
														Body(
															app.Text("func Div() HTMLDiv"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDl").
														Body(
															app.Text("type HTMLDl"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dl").
														Body(
															app.Text("func Dl() HTMLDl"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLDt").
														Body(
															app.Text("type HTMLDt"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Dt").
														Body(
															app.Text("func Dt() HTMLDt"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLEm").
														Body(
															app.Text("type HTMLEm"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Em").
														Body(
															app.Text("func Em() HTMLEm"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLEmbed").
														Body(
															app.Text("type HTMLEmbed"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Embed").
														Body(
															app.Text("func Embed() HTMLEmbed"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLFieldSet").
														Body(
															app.Text("type HTMLFieldSet"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#FieldSet").
														Body(
															app.Text("func FieldSet() HTMLFieldSet"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLFigCaption").
														Body(
															app.Text("type HTMLFigCaption"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#FigCaption").
														Body(
															app.Text("func FigCaption() HTMLFigCaption"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLFigure").
														Body(
															app.Text("type HTMLFigure"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Figure").
														Body(
															app.Text("func Figure() HTMLFigure"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLFooter").
														Body(
															app.Text("type HTMLFooter"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Footer").
														Body(
															app.Text("func Footer() HTMLFooter"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLForm").
														Body(
															app.Text("type HTMLForm"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Form").
														Body(
															app.Text("func Form() HTMLForm"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLH1").
														Body(
															app.Text("type HTMLH1"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#H1").
														Body(
															app.Text("func H1() HTMLH1"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLH2").
														Body(
															app.Text("type HTMLH2"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#H2").
														Body(
															app.Text("func H2() HTMLH2"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLH3").
														Body(
															app.Text("type HTMLH3"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#H3").
														Body(
															app.Text("func H3() HTMLH3"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLH4").
														Body(
															app.Text("type HTMLH4"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#H4").
														Body(
															app.Text("func H4() HTMLH4"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLH5").
														Body(
															app.Text("type HTMLH5"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#H5").
														Body(
															app.Text("func H5() HTMLH5"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLH6").
														Body(
															app.Text("type HTMLH6"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#H6").
														Body(
															app.Text("func H6() HTMLH6"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLHead").
														Body(
															app.Text("type HTMLHead"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Head").
														Body(
															app.Text("func Head() HTMLHead"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLHeader").
														Body(
															app.Text("type HTMLHeader"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Header").
														Body(
															app.Text("func Header() HTMLHeader"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLHr").
														Body(
															app.Text("type HTMLHr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Hr").
														Body(
															app.Text("func Hr() HTMLHr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLHtml").
														Body(
															app.Text("type HTMLHtml"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Html").
														Body(
															app.Text("func Html() HTMLHtml"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLI").
														Body(
															app.Text("type HTMLI"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#I").
														Body(
															app.Text("func I() HTMLI"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLIFrame").
														Body(
															app.Text("type HTMLIFrame"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#IFrame").
														Body(
															app.Text("func IFrame() HTMLIFrame"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLImg").
														Body(
															app.Text("type HTMLImg"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Img").
														Body(
															app.Text("func Img() HTMLImg"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLInput").
														Body(
															app.Text("type HTMLInput"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Input").
														Body(
															app.Text("func Input() HTMLInput"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLIns").
														Body(
															app.Text("type HTMLIns"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Ins").
														Body(
															app.Text("func Ins() HTMLIns"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLKbd").
														Body(
															app.Text("type HTMLKbd"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Kbd").
														Body(
															app.Text("func Kbd() HTMLKbd"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLLabel").
														Body(
															app.Text("type HTMLLabel"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Label").
														Body(
															app.Text("func Label() HTMLLabel"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLLegends").
														Body(
															app.Text("type HTMLLegends"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Legends").
														Body(
															app.Text("func Legends() HTMLLegends"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLLi").
														Body(
															app.Text("type HTMLLi"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Li").
														Body(
															app.Text("func Li() HTMLLi"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLLink").
														Body(
															app.Text("type HTMLLink"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Link").
														Body(
															app.Text("func Link() HTMLLink"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLMain").
														Body(
															app.Text("type HTMLMain"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Main").
														Body(
															app.Text("func Main() HTMLMain"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLMap").
														Body(
															app.Text("type HTMLMap"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Map").
														Body(
															app.Text("func Map() HTMLMap"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLMark").
														Body(
															app.Text("type HTMLMark"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Mark").
														Body(
															app.Text("func Mark() HTMLMark"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLMeta").
														Body(
															app.Text("type HTMLMeta"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Meta").
														Body(
															app.Text("func Meta() HTMLMeta"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLMeter").
														Body(
															app.Text("type HTMLMeter"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Meter").
														Body(
															app.Text("func Meter() HTMLMeter"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLNav").
														Body(
															app.Text("type HTMLNav"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Nav").
														Body(
															app.Text("func Nav() HTMLNav"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLNoScript").
														Body(
															app.Text("type HTMLNoScript"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#NoScript").
														Body(
															app.Text("func NoScript() HTMLNoScript"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLObject").
														Body(
															app.Text("type HTMLObject"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Object").
														Body(
															app.Text("func Object() HTMLObject"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLOl").
														Body(
															app.Text("type HTMLOl"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Ol").
														Body(
															app.Text("func Ol() HTMLOl"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLOptGroup").
														Body(
															app.Text("type HTMLOptGroup"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#OptGroup").
														Body(
															app.Text("func OptGroup() HTMLOptGroup"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLOption").
														Body(
															app.Text("type HTMLOption"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Option").
														Body(
															app.Text("func Option() HTMLOption"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLOutput").
														Body(
															app.Text("type HTMLOutput"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Output").
														Body(
															app.Text("func Output() HTMLOutput"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLP").
														Body(
															app.Text("type HTMLP"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#P").
														Body(
															app.Text("func P() HTMLP"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLParam").
														Body(
															app.Text("type HTMLParam"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Param").
														Body(
															app.Text("func Param() HTMLParam"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLPicture").
														Body(
															app.Text("type HTMLPicture"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Picture").
														Body(
															app.Text("func Picture() HTMLPicture"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLPre").
														Body(
															app.Text("type HTMLPre"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Pre").
														Body(
															app.Text("func Pre() HTMLPre"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLProgress").
														Body(
															app.Text("type HTMLProgress"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Progress").
														Body(
															app.Text("func Progress() HTMLProgress"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLQ").
														Body(
															app.Text("type HTMLQ"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Q").
														Body(
															app.Text("func Q() HTMLQ"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLRp").
														Body(
															app.Text("type HTMLRp"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Rp").
														Body(
															app.Text("func Rp() HTMLRp"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLRt").
														Body(
															app.Text("type HTMLRt"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Rt").
														Body(
															app.Text("func Rt() HTMLRt"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLRuby").
														Body(
															app.Text("type HTMLRuby"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Ruby").
														Body(
															app.Text("func Ruby() HTMLRuby"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLS").
														Body(
															app.Text("type HTMLS"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#S").
														Body(
															app.Text("func S() HTMLS"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSamp").
														Body(
															app.Text("type HTMLSamp"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Samp").
														Body(
															app.Text("func Samp() HTMLSamp"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLScript").
														Body(
															app.Text("type HTMLScript"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Script").
														Body(
															app.Text("func Script() HTMLScript"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSection").
														Body(
															app.Text("type HTMLSection"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Section").
														Body(
															app.Text("func Section() HTMLSection"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSelect").
														Body(
															app.Text("type HTMLSelect"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Select").
														Body(
															app.Text("func Select() HTMLSelect"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSmall").
														Body(
															app.Text("type HTMLSmall"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Small").
														Body(
															app.Text("func Small() HTMLSmall"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSource").
														Body(
															app.Text("type HTMLSource"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Source").
														Body(
															app.Text("func Source() HTMLSource"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSpan").
														Body(
															app.Text("type HTMLSpan"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Span").
														Body(
															app.Text("func Span() HTMLSpan"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLStrong").
														Body(
															app.Text("type HTMLStrong"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Strong").
														Body(
															app.Text("func Strong() HTMLStrong"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLStyle").
														Body(
															app.Text("type HTMLStyle"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Style").
														Body(
															app.Text("func Style() HTMLStyle"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSub").
														Body(
															app.Text("type HTMLSub"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Sub").
														Body(
															app.Text("func Sub() HTMLSub"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSummary").
														Body(
															app.Text("type HTMLSummary"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Summary").
														Body(
															app.Text("func Summary() HTMLSummary"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLSup").
														Body(
															app.Text("type HTMLSup"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Sup").
														Body(
															app.Text("func Sup() HTMLSup"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTBody").
														Body(
															app.Text("type HTMLTBody"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#TBody").
														Body(
															app.Text("func TBody() HTMLTBody"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTHead").
														Body(
															app.Text("type HTMLTHead"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#THead").
														Body(
															app.Text("func THead() HTMLTHead"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTable").
														Body(
															app.Text("type HTMLTable"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Table").
														Body(
															app.Text("func Table() HTMLTable"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTd").
														Body(
															app.Text("type HTMLTd"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Td").
														Body(
															app.Text("func Td() HTMLTd"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTemplate").
														Body(
															app.Text("type HTMLTemplate"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Template").
														Body(
															app.Text("func Template() HTMLTemplate"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTextarea").
														Body(
															app.Text("type HTMLTextarea"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Textarea").
														Body(
															app.Text("func Textarea() HTMLTextarea"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTfoot").
														Body(
															app.Text("type HTMLTfoot"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Tfoot").
														Body(
															app.Text("func Tfoot() HTMLTfoot"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTh").
														Body(
															app.Text("type HTMLTh"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Th").
														Body(
															app.Text("func Th() HTMLTh"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTime").
														Body(
															app.Text("type HTMLTime"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Time").
														Body(
															app.Text("func Time() HTMLTime"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTitle").
														Body(
															app.Text("type HTMLTitle"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Title").
														Body(
															app.Text("func Title() HTMLTitle"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLTr").
														Body(
															app.Text("type HTMLTr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Tr").
														Body(
															app.Text("func Tr() HTMLTr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLU").
														Body(
															app.Text("type HTMLU"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#U").
														Body(
															app.Text("func U() HTMLU"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLUl").
														Body(
															app.Text("type HTMLUl"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Ul").
														Body(
															app.Text("func Ul() HTMLUl"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLVar").
														Body(
															app.Text("type HTMLVar"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Var").
														Body(
															app.Text("func Var() HTMLVar"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLVideo").
														Body(
															app.Text("type HTMLVideo"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Video").
														Body(
															app.Text("func Video() HTMLVideo"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#HTMLWbr").
														Body(
															app.Text("type HTMLWbr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Wbr").
														Body(
															app.Text("func Wbr() HTMLWbr"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Handler").
														Body(
															app.Text("type Handler"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Handler.ServeHTTP").
														Body(
															app.Text("func (h *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request)"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Icon").
														Body(
															app.Text("type Icon"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Kind").
														Body(
															app.Text("type Kind"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Kind.String").
														Body(
															app.Text("func (k Kind) String() string"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#MenuItemNode").
														Body(
															app.Text("type MenuItemNode"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#MenuItem").
														Body(
															app.Text("func MenuItem() MenuItemNode"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Mounter").
														Body(
															app.Text("type Mounter"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Navigator").
														Body(
															app.Text("type Navigator"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#RangeLoop").
														Body(
															app.Text("type RangeLoop"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Range").
														Body(
															app.Text("func Range(src interface{}) RangeLoop"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#ResourceProvider").
														Body(
															app.Text("type ResourceProvider"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#GitHubPages").
														Body(
															app.Text("func GitHubPages(repoName string) ResourceProvider"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#LocalDir").
														Body(
															app.Text("func LocalDir(path string) ResourceProvider"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#RemoteBucket").
														Body(
															app.Text("func RemoteBucket(url string) ResourceProvider"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#TestUIDescriptor").
														Body(
															app.Text("type TestUIDescriptor"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Type").
														Body(
															app.Text("type Type"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#UI").
														Body(
															app.Text("type UI"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#FilterUIElems").
														Body(
															app.Text("func FilterUIElems(uis ...UI) []UI"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Raw").
														Body(
															app.Text("func Raw(v string) UI"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Text").
														Body(
															app.Text("func Text(v interface{}) UI"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#UIShell").
														Body(
															app.Text("type UIShell"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Shell").
														Body(
															app.Text("func Shell() UIShell"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Value").
														Body(
															app.Text("type Value"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Null").
														Body(
															app.Text("func Null() Value"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Undefined").
														Body(
															app.Text("func Undefined() Value"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#ValueOf").
														Body(
															app.Text("func ValueOf(x interface{}) Value"),
														),
												),
												app.Dd().Body(
													app.A().
														Href("#Wrapper").
														Body(
															app.Text("type Wrapper"),
														),
												),
											),
										),
									app.H3().Body(
										app.Text("Package files"),
									),
									app.P().Body(
										app.Span().
											Style("font-size", "90%").
											Body(
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go").
													Body(
														app.Text("app.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app_nowasm.go").
													Body(
														app.Text("app_nowasm.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go").
													Body(
														app.Text("component.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/concurrency.go").
													Body(
														app.Text("concurrency.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/condition.go").
													Body(
														app.Text("condition.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/context.go").
													Body(
														app.Text("context.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/contextmenu.go").
													Body(
														app.Text("contextmenu.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/element.go").
													Body(
														app.Text("element.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go").
													Body(
														app.Text("html.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/http.go").
													Body(
														app.Text("http.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go").
													Body(
														app.Text("js.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js_nowasm.go").
													Body(
														app.Text("js_nowasm.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/log.go").
													Body(
														app.Text("log.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/node.go").
													Body(
														app.Text("node.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/notfound.go").
													Body(
														app.Text("notfound.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/range.go").
													Body(
														app.Text("range.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/raw.go").
													Body(
														app.Text("raw.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/resource.go").
													Body(
														app.Text("resource.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/route.go").
													Body(
														app.Text("route.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/scripts.go").
													Body(
														app.Text("scripts.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/shell.go").
													Body(
														app.Text("shell.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/static.go").
													Body(
														app.Text("static.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/storage.go").
													Body(
														app.Text("storage.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/storage_nowasm.go").
													Body(
														app.Text("storage_nowasm.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/strings.go").
													Body(
														app.Text("strings.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/testing.go").
													Body(
														app.Text("testing.go"),
													),
												app.A().
													Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/text.go").
													Body(
														app.Text("text.go"),
													),
											),
									),
								),
						),
					app.H2().
						ID("pkg-variables").
						Body(
							app.Text("Variables"),
						),
					app.Pre().Body(
						app.Text("var ("),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DefaultLogger is the logger used to log info and errors."),
							),
						app.Span().
							ID("DefaultLogger").
							Body(
								app.Text("DefaultLogger"),
							),
						app.Text("func(format"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v ...interface{}) = log\n)"),
					),
					app.H2().
						ID("CopyBytesToGo").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=6902:6947#L203").
								Body(
									app.Text("CopyBytesToGo"),
								),
							app.A().
								Class("permalink").
								Href("#CopyBytesToGo").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func CopyBytesToGo(dst []"),
						app.A().
							Href("/pkg/builtin/#byte").
							Body(
								app.Text("byte"),
							),
						app.Text(", src"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text(")"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
					),
					app.P().Body(
						app.Text("CopyBytesToGo copies bytes from the Uint8Array src to dst. It returns the\nnumber of bytes copied, which will be the minimum of the lengths of src and\ndst."),
					),
					app.P().Body(
						app.Text("CopyBytesToGo panics if src is not an Uint8Array."),
					),
					app.H2().
						ID("CopyBytesToJS").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=7205:7250#L212").
								Body(
									app.Text("CopyBytesToJS"),
								),
							app.A().
								Class("permalink").
								Href("#CopyBytesToJS").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func CopyBytesToJS(dst"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text(", src []"),
						app.A().
							Href("/pkg/builtin/#byte").
							Body(
								app.Text("byte"),
							),
						app.Text(")"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
					),
					app.P().Body(
						app.Text("CopyBytesToJS copies bytes from src to the Uint8Array dst. It returns the\nnumber of bytes copied, which will be the minimum of the lengths of src and\ndst."),
					),
					app.P().Body(
						app.Text("CopyBytesToJS panics if dst is not an Uint8Array."),
					),
					app.H2().
						ID("Dispatch").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/concurrency.go?s=289:312#L3").
								Body(
									app.Text("Dispatch"),
								),
							app.A().
								Class("permalink").
								Href("#Dispatch").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Dispatch(f func())"),
					),
					app.P().Body(
						app.Text("Dispatch executes the given function on the UI goroutine."),
					),
					app.H2().
						ID("GenerateStaticWebsite").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/static.go?s=434:490#L10").
								Body(
									app.Text("GenerateStaticWebsite"),
								),
							app.A().
								Class("permalink").
								Href("#GenerateStaticWebsite").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func GenerateStaticWebsite(dir"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", h *"),
						app.A().
							Href("#Handler").
							Body(
								app.Text("Handler"),
							),
						app.Text(")"),
						app.A().
							Href("/pkg/builtin/#error").
							Body(
								app.Text("error"),
							),
					),
					app.P().Body(
						app.Text("GenerateStaticWebsite generates the files to run a PWA built with go-app as a\nstatic website in the specified directory. Static websites can be used with\nhosts such as Github Pages."),
					),
					app.P().Body(
						app.Text("Note that app.wasm must still be built separately and put into the web\ndirectory."),
					),
					app.H2().
						ID("Getenv").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=381:409#L10").
								Body(
									app.Text("Getenv"),
								),
							app.A().
								Class("permalink").
								Href("#Getenv").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Getenv(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
					),
					app.P().Body(
						app.Text("Getenv retrieves the value of the environment variable named by the key. It\nreturns the value, which will be empty if the variable is not present."),
					),
					app.H2().
						ID("KeepBodyClean").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=529:564#L16").
								Body(
									app.Text("KeepBodyClean"),
								),
							app.A().
								Class("permalink").
								Href("#KeepBodyClean").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func KeepBodyClean() (close func())"),
					),
					app.P().Body(
						app.Text("KeepBodyClean prevents third-party Javascript libraries to add nodes to the\nbody element."),
					),
					app.H2().
						ID("Log").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/log.go?s=464:505#L18").
								Body(
									app.Text("Log"),
								),
							app.A().
								Class("permalink").
								Href("#Log").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Log(format"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v ...interface{})"),
					),
					app.P().Body(
						app.Text("Log logs according to a format specifier using the default logger."),
					),
					app.H2().
						ID("Navigate").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=634:662#L21").
								Body(
									app.Text("Navigate"),
								),
							app.A().
								Class("permalink").
								Href("#Navigate").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Navigate(rawurl"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
					),
					app.P().Body(
						app.Text("Navigate navigates to the given URL."),
					),
					app.H2().
						ID("NewContextMenu").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=1115:1161#L45").
								Body(
									app.Text("NewContextMenu"),
								),
							app.A().
								Class("permalink").
								Href("#NewContextMenu").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func NewContextMenu(menuItems ..."),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Text(")"),
					),
					app.P().Body(
						app.Text("NewContextMenu displays a context menu filled with the given menu items."),
					),
					app.H2().
						ID("Reload").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=1257:1270#L52").
								Body(
									app.Text("Reload"),
								),
							app.A().
								Class("permalink").
								Href("#Reload").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Reload()"),
					),
					app.P().Body(
						app.Text("Reload reloads the current page."),
					),
					app.H2().
						ID("Route").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/route.go?s=110:142#L1").
								Body(
									app.Text("Route"),
								),
							app.A().
								Class("permalink").
								Href("#Route").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Route(path"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", node"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
					),
					app.P().Body(
						app.Text("Route binds the requested path to the given UI node."),
					),
					app.H2().
						ID("RouteWithRegexp").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/route.go?s=299:344#L6").
								Body(
									app.Text("RouteWithRegexp"),
								),
							app.A().
								Class("permalink").
								Href("#RouteWithRegexp").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func RouteWithRegexp(pattern"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", node"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
					),
					app.P().Body(
						app.Text("RouteWithRegexp binds the regular expression pattern to the given UI node.\nPatterns use the Go standard regexp format."),
					),
					app.H2().
						ID("Run").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=1453:1463#L62").
								Body(
									app.Text("Run"),
								),
							app.A().
								Class("permalink").
								Href("#Run").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Run()"),
					),
					app.P().Body(
						app.Text("Run starts the wasm app and displays the UI node associated with the\nrequested URL path."),
					),
					app.P().Body(
						app.Text("It panics if Go architecture is not wasm."),
					),
					app.H2().
						ID("StaticResource").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=1799:1838#L73").
								Body(
									app.Text("StaticResource"),
								),
							app.A().
								Class("permalink").
								Href("#StaticResource").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func StaticResource(path"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
					),
					app.P().Body(
						app.Text("StaticResource makes a static resource path point to the right\nlocation whether the root directory is remote or not."),
					),
					app.P().Body(
						app.Text("Static resources are resources located in the web directory."),
					),
					app.P().Body(
						app.Text("This call is used internally to resolve paths within Cite, Data, Href, Src,\nand SrcSet. Paths already resolved are skipped."),
					),
					app.H2().
						ID("TestMatch").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/testing.go?s=1914:1963#L63").
								Body(
									app.Text("TestMatch"),
								),
							app.A().
								Class("permalink").
								Href("#TestMatch").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func TestMatch(tree"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(", d"),
						app.A().
							Href("#TestUIDescriptor").
							Body(
								app.Text("TestUIDescriptor"),
							),
						app.Text(")"),
						app.A().
							Href("/pkg/builtin/#error").
							Body(
								app.Text("error"),
							),
					),
					app.P().Body(
						app.Text("TestMatch looks for the element targeted by the descriptor in the given tree\nand reports whether it matches with the expected element."),
					),
					app.P().Body(
						app.Text("Eg:"),
					),
					app.Pre().Body(
						app.Text("tree := app.Div().Body(\n    app.H2().Body(\n        app.Text(\"foo\"),\n    ),\n    app.P().Body(\n        app.Text(\"bar\"),\n    ),\n)\n\n// Testing root:\nerr := app.TestMatch(tree, app.TestUIDescriptor{\n    Path:     TestPath(),\n    Expected: app.Div(),\n})\n// OK => err == nil\n\n// Testing h2:\nerr := app.TestMatch(tree, app.TestUIDescriptor{\n    Path:     TestPath(0),\n    Expected: app.H3(),\n})\n// KO => err != nil because we ask h2 to match with h3\n\n// Testing text from p:\nerr = app.TestMatch(tree, app.TestUIDescriptor{\n    Path:     TestPath(1, 0),\n    Expected: app.Text(\"bar\"),\n})\n// OK => err == nil"),
					),
					app.H2().
						ID("TestPath").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/testing.go?s=1009:1038#L26").
								Body(
									app.Text("TestPath"),
								),
							app.A().
								Class("permalink").
								Href("#TestPath").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func TestPath(p ..."),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(") []"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
					),
					app.P().Body(
						app.Text("TestPath is a helper function that returns a path to use in a\nTestUIDescriptor."),
					),
					app.H2().
						ID("BrowserStorage").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/storage.go?s=457:885#L5").
								Body(
									app.Text("BrowserStorage"),
								),
							app.A().
								Class("permalink").
								Href("#BrowserStorage").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("BrowserStorage is the interface that describes a web browser storage."),
					),
					app.Pre().Body(
						app.Text("type BrowserStorage interface {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Set sets the value to the given key. The value must be json convertible."),
							),
						app.Text("Set(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("/pkg/builtin/#error").
							Body(
								app.Text("error"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Get gets the item associated to the given key and store it in the given"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// value."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// It returns an error if v is not a pointer."),
							),
						app.Text("Get(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("/pkg/builtin/#error").
							Body(
								app.Text("error"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Del deletes the item associated with the given key."),
							),
						app.Text("Del(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Clear deletes all items."),
							),
						app.Text("Clear()\n}"),
					),
					app.Pre().Body(
						app.Text("var ("),
						app.Span().
							Class("comment").
							Body(
								app.Text("// LocalStorage is a storage that uses the browser local storage associated"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// to the document origin. Data stored has no expiration time."),
							),
						app.Span().
							ID("LocalStorage").
							Body(
								app.Text("LocalStorage"),
							),
						app.A().
							Href("#BrowserStorage").
							Body(
								app.Text("BrowserStorage"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// SessionStorage is a storage that uses the browser session storage"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// associated to the document origin. Data stored expire when the page"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// session ends."),
							),
						app.Span().
							ID("SessionStorage").
							Body(
								app.Text("SessionStorage"),
							),
						app.A().
							Href("#BrowserStorage").
							Body(
								app.Text("BrowserStorage"),
							),
						app.Text(")"),
					),
					app.H2().
						ID("BrowserWindow").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=5724:6405#L161").
								Body(
									app.Text("BrowserWindow"),
								),
							app.A().
								Class("permalink").
								Href("#BrowserWindow").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("BrowserWindow is the interface that describes the browser window."),
					),
					app.Pre().Body(
						app.Text("type BrowserWindow interface {"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The window current url (window.location.href)."),
							),
						app.Text("URL() *"),
						app.A().
							Href("/pkg/net/url/").
							Body(
								app.Text("url"),
							),
						app.Text("."),
						app.A().
							Href("/pkg/net/url/#URL").
							Body(
								app.Text("URL"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The window size."),
							),
						app.Text("Size() (w, h"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The position of the cursor (mouse or touch)."),
							),
						app.Text("CursorPosition() (x, y"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Returns the HTML element with the id property that matches the given id."),
							),
						app.Text("GetElementByID(id"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Scrolls to the HTML element with the given id."),
							),
						app.Text("ScrollToID(id"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AddEventListener subscribes a given handler to the specified event. It"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// returns a function that must be called to unsubscribe the handler and"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// release allocated resources."),
							),
						app.Text("AddEventListener(event"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(") func()"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// contains filtered or unexported methods"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Window").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/app.go?s=2084:2111#L87").
								Body(
									app.Text("Window"),
								),
							app.A().
								Class("permalink").
								Href("#Window").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Window()"),
						app.A().
							Href("#BrowserWindow").
							Body(
								app.Text("BrowserWindow"),
							),
					),
					app.P().Body(
						app.Text("Window returns the JavaScript \"window\" object."),
					),
					app.H2().
						ID("Compo").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=1669:1788#L58").
								Body(
									app.Text("Compo"),
								),
							app.A().
								Class("permalink").
								Href("#Compo").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Compo represents the base struct to use in order to build a component."),
					),
					app.Pre().Body(
						app.Text("type Compo struct {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// contains filtered or unexported fields"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Compo.JSValue").
						Body(
							app.Text("func (*Compo)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=1941:1972#L72").
								Body(
									app.Text("JSValue"),
								),
							app.A().
								Class("permalink").
								Href("#Compo.JSValue").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (c *"),
						app.A().
							Href("#Compo").
							Body(
								app.Text("Compo"),
							),
						app.Text(") JSValue()"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
					),
					app.P().Body(
						app.Text("JSValue returns the javascript value of the component root."),
					),
					app.H3().
						ID("Compo.Kind").
						Body(
							app.Text("func (*Compo)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=1827:1854#L67").
								Body(
									app.Text("Kind"),
								),
							app.A().
								Class("permalink").
								Href("#Compo.Kind").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (c *"),
						app.A().
							Href("#Compo").
							Body(
								app.Text("Compo"),
							),
						app.Text(") Kind()"),
						app.A().
							Href("#Kind").
							Body(
								app.Text("Kind"),
							),
					),
					app.P().Body(
						app.Text("Kind returns the ui element kind."),
					),
					app.H3().
						ID("Compo.Mounted").
						Body(
							app.Text("func (*Compo)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=2056:2086#L77").
								Body(
									app.Text("Mounted"),
								),
							app.A().
								Class("permalink").
								Href("#Compo.Mounted").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (c *"),
						app.A().
							Href("#Compo").
							Body(
								app.Text("Compo"),
							),
						app.Text(") Mounted()"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
					),
					app.P().Body(
						app.Text("Mounted reports whether the component is mounted."),
					),
					app.H3().
						ID("Compo.Render").
						Body(
							app.Text("func (*Compo)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=2367:2394#L86").
								Body(
									app.Text("Render"),
								),
							app.A().
								Class("permalink").
								Href("#Compo.Render").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (c *"),
						app.A().
							Href("#Compo").
							Body(
								app.Text("Compo"),
							),
						app.Text(") Render()"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
					),
					app.P().Body(
						app.Text("Render describes the component content. This is a default implementation to\nsatisfy the app.Composer interface. It should be redefined when app.Compo is\nembedded."),
					),
					app.H3().
						ID("Compo.Update").
						Body(
							app.Text("func (*Compo)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=2985:3009#L106").
								Body(
									app.Text("Update"),
								),
							app.A().
								Class("permalink").
								Href("#Compo.Update").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (c *"),
						app.A().
							Href("#Compo").
							Body(
								app.Text("Compo"),
							),
						app.Text(") Update()"),
					),
					app.P().Body(
						app.Text("Update triggers a component appearance update. It should be called when a\nfield used to render the component has been modified. Updates are always\nperformed on the UI goroutine."),
					),
					app.H2().
						ID("Composer").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=482:744#L16").
								Body(
									app.Text("Composer"),
								),
							app.A().
								Class("permalink").
								Href("#Composer").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Composer is the interface that describes a customized, independent and\nreusable UI element."),
					),
					app.P().Body(
						app.Text("Satisfying this interface is done by embedding app.Compo into a struct and\nimplementing the Render function."),
					),
					app.P().Body(
						app.Text("Example:"),
					),
					app.Pre().Body(
						app.Text("type Hello struct {\n    app.Compo\n}\n\nfunc (c *Hello) Render() app.UI {\n    return app.Text(\"hello\")\n}"),
					),
					app.Pre().Body(
						app.Text("type Composer interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Render returns the node tree that define how the component is desplayed."),
							),
						app.Text("Render()"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Update update the component appearance. It should be called when a field"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// used to render the component has been modified."),
							),
						app.Text("Update()\n}"),
					),
					app.H2().
						ID("Condition").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/condition.go?s=201:525#L2").
								Body(
									app.Text("Condition"),
								),
							app.A().
								Class("permalink").
								Href("#Condition").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Condition represents a control structure that displays nodes depending on a\ngiven expression."),
					),
					app.Pre().Body(
						app.Text("type Condition interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ElseIf sets the condition with the given nodes if previous expressions"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// were not met and given expression is true."),
							),
						app.Text("ElseIf(expr"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(", elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#Condition").
							Body(
								app.Text("Condition"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Else sets the condition with the given UI elements if previous"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// expressions were not met."),
							),
						app.Text("Else(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#Condition").
							Body(
								app.Text("Condition"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("If").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/condition.go?s=623:664#L16").
								Body(
									app.Text("If"),
								),
							app.A().
								Class("permalink").
								Href("#If").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func If(expr"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(", elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#Condition").
							Body(
								app.Text("Condition"),
							),
					),
					app.P().Body(
						app.Text("If returns a condition that filters the given elements according to the given\nexpression."),
					),
					app.H2().
						ID("Context").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/context.go?s=238:454#L1").
								Body(
									app.Text("Context"),
								),
							app.A().
								Class("permalink").
								Href("#Context").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Context represents a context that is tied to a UI element. It is canceled\nwhen the element is dismounted."),
					),
					app.P().Body(
						app.Text("It implements the context.Context interface."),
					),
					app.Pre().Body(
						app.A().
							Href("https://golang.org/pkg/context/#Context").
							Body(
								app.Text("https://golang.org/pkg/context/#Context"),
							),
					),
					app.Pre().Body(
						app.Text("type Context struct {"),
						app.A().
							Href("/pkg/context/").
							Body(
								app.Text("context"),
							),
						app.Text("."),
						app.A().
							Href("/pkg/context/#Context").
							Body(
								app.Text("Context"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The UI element tied to the context."),
							),
						app.Span().
							ID("Context.Src").
							Body(),
						app.Text("Src"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The JavaScript value of the element tied to the context. This is a"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// shorthand for:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  ctx.Src.JSValue()"),
							),
						app.Span().
							ID("Context.JSSrc").
							Body(),
						app.Text("JSSrc"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text("}"),
					),
					app.H2().
						ID("Dismounter").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=1133:1298#L39").
								Body(
									app.Text("Dismounter"),
								),
							app.A().
								Class("permalink").
								Href("#Dismounter").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Dismounter is the interface that describes a component that can perform\nadditional actions when dismounted."),
					),
					app.Pre().Body(
						app.Text("type Dismounter interface {"),
						app.A().
							Href("#Composer").
							Body(
								app.Text("Composer"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The function that is called when the component is dismounted. It is"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// always called on the UI goroutine."),
							),
						app.Text("OnDismount()\n}"),
					),
					app.H2().
						ID("Dispatcher").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/concurrency.go?s=198:226#L1").
								Body(
									app.Text("Dispatcher"),
								),
							app.A().
								Class("permalink").
								Href("#Dispatcher").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Dispatcher is a function that executes the given function on the goroutine\ndedicated to UI."),
					),
					app.Pre().Body(
						app.Text("type Dispatcher func(func())"),
					),
					app.H2().
						ID("Environment").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/http.go?s=15454:15488#L647").
								Body(
									app.Text("Environment"),
								),
							app.A().
								Class("permalink").
								Href("#Environment").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Environment describes the environment variables to pass to the progressive\nweb app."),
					),
					app.Pre().Body(
						app.Text("type Environment map["),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text("]"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
					),
					app.H2().
						ID("Event").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=6468:6496#L188").
								Body(
									app.Text("Event"),
								),
							app.A().
								Class("permalink").
								Href("#Event").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Event is the interface that describes a javascript event."),
					),
					app.Pre().Body(
						app.Text("type Event struct {"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Event.PreventDefault").
						Body(
							app.Text("func (Event)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=6619:6650#L194").
								Body(
									app.Text("PreventDefault"),
								),
							app.A().
								Class("permalink").
								Href("#Event.PreventDefault").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (e"),
						app.A().
							Href("#Event").
							Body(
								app.Text("Event"),
							),
						app.Text(") PreventDefault()"),
					),
					app.P().Body(
						app.Text("PreventDefault cancels the event if it is cancelable. The default action that\nbelongs to the event will not occur."),
					),
					app.H2().
						ID("EventHandler").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/node.go?s=2581:2625#L117").
								Body(
									app.Text("EventHandler"),
								),
							app.A().
								Class("permalink").
								Href("#EventHandler").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("EventHandler represents a function that can handle HTML events. They are\nalways called on the UI goroutine."),
					),
					app.Pre().Body(
						app.Text("type EventHandler func(ctx"),
						app.A().
							Href("#Context").
							Body(
								app.Text("Context"),
							),
						app.Text(", e"),
						app.A().
							Href("#Event").
							Body(
								app.Text("Event"),
							),
						app.Text(")"),
					),
					app.H2().
						ID("Func").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=4522:4683#L132").
								Body(
									app.Text("Func"),
								),
							app.A().
								Class("permalink").
								Href("#Func").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Func is the interface that describes a wrapped Go function to be called by\nJavaScript."),
					),
					app.Pre().Body(
						app.Text("type Func interface {"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Release frees up resources allocated for the function. The function must"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// not be invoked after calling Release."),
							),
						app.Text("Release()\n}"),
					),
					app.H3().
						ID("FuncOf").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=5567:5630#L156").
								Body(
									app.Text("FuncOf"),
								),
							app.A().
								Class("permalink").
								Href("#FuncOf").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func FuncOf(fn func(this"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text(", args []"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text(") interface{})"),
						app.A().
							Href("#Func").
							Body(
								app.Text("Func"),
							),
					),
					app.P().Body(
						app.Text("FuncOf returns a wrapped function."),
					),
					app.P().Body(
						app.Text("Invoking the JavaScript function will synchronously call the Go function fn\nwith the value of JavaScript's \"this\" keyword and the arguments of the\ninvocation. The return value of the invocation is the result of the Go\nfunction mapped back to JavaScript according to ValueOf."),
					),
					app.P().Body(
						app.Text("A wrapped function triggered during a call from Go to JavaScript gets\nexecuted on the same goroutine. A wrapped function triggered by JavaScript's\nevent loop gets executed on an extra goroutine. Blocking operations in the\nwrapped function will block the event loop. As a consequence, if one wrapped\nfunction blocks, other wrapped funcs will not be processed. A blocking\nfunction should therefore explicitly start a new goroutine."),
					),
					app.P().Body(
						app.Text("Func.Release must be called to free up resources when the function will not\nbe used any more."),
					),
					app.H2().
						ID("HTMLA").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=141:6196#L1").
								Body(
									app.Text("HTMLA"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLA").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLA is the interface that describes a <a> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLA interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Download specifies that the target will be downloaded when a user clicks on the hyperlink."),
							),
						app.Text("Download(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Href specifies the URL of the page the link goes to."),
							),
						app.Text("Href(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// HrefLang specifies the language of the linked document."),
							),
						app.Text("HrefLang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Media specifies what media/device the linked document is optimized for."),
							),
						app.Text("Media(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Ping specifies a list of URLs to be notified if the user follows the hyperlink."),
							),
						app.Text("Ping(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Rel specifies the relationship between the current document and the linked document."),
							),
						app.Text("Rel(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Target specifies the target for where to open the linked document or where to submit the form."),
							),
						app.Text("Target(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("A").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=6253:6267#L173").
								Body(
									app.Text("A"),
								),
							app.A().
								Class("permalink").
								Href("#A").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func A()"),
						app.A().
							Href("#HTMLA").
							Body(
								app.Text("HTMLA"),
							),
					),
					app.P().Body(
						app.Text("A returns an HTML element that defines a hyperlink."),
					),
					app.H2().
						ID("HTMLAbbr").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=11491:16901#L473").
								Body(
									app.Text("HTMLAbbr"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLAbbr").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLAbbr is the interface that describes a <abbr> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLAbbr interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Abbr").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=16979:16999#L622").
								Body(
									app.Text("Abbr"),
								),
							app.A().
								Class("permalink").
								Href("#Abbr").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Abbr()"),
						app.A().
							Href("#HTMLAbbr").
							Body(
								app.Text("HTMLAbbr"),
							),
					),
					app.P().Body(
						app.Text("Abbr returns an HTML element that defines an abbreviation or an acronym."),
					),
					app.H2().
						ID("HTMLAddress").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=21900:27457#L882").
								Body(
									app.Text("HTMLAddress"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLAddress").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLAddress is the interface that describes a <address> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLAddress interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Address").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=27563:27589#L1031").
								Body(
									app.Text("Address"),
								),
							app.A().
								Class("permalink").
								Href("#Address").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Address()"),
						app.A().
							Href("#HTMLAddress").
							Body(
								app.Text("HTMLAddress"),
							),
					),
					app.P().Body(
						app.Text("Address returns an HTML element that defines contact information for the author/owner of a document."),
					),
					app.H2().
						ID("HTMLArea").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=32781:38951#L1291").
								Body(
									app.Text("HTMLArea"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLArea").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLArea is the interface that describes a <area> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLArea interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Alt specifies an alternate text when the original element fails to display."),
							),
						app.Text("Alt(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Coords specifies the coordinates of the area."),
							),
						app.Text("Coords(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Download specifies that the target will be downloaded when a user clicks on the hyperlink."),
							),
						app.Text("Download(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Href specifies the URL of the page the link goes to."),
							),
						app.Text("Href(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// HrefLang specifies the language of the linked document."),
							),
						app.Text("HrefLang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Media specifies what media/device the linked document is optimized for."),
							),
						app.Text("Media(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Rel specifies the relationship between the current document and the linked document."),
							),
						app.Text("Rel(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Shape specifies the shape of the area."),
							),
						app.Text("Shape(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Target specifies the target for where to open the linked document or where to submit the form."),
							),
						app.Text("Target(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Area").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=39027:39047#L1464").
								Body(
									app.Text("Area"),
								),
							app.A().
								Class("permalink").
								Href("#Area").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Area()"),
						app.A().
							Href("#HTMLArea").
							Body(
								app.Text("HTMLArea"),
							),
					),
					app.P().Body(
						app.Text("Area returns an HTML element that defines an area inside an image-map."),
					),
					app.H2().
						ID("HTMLArticle").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=44626:50183#L1765").
								Body(
									app.Text("HTMLArticle"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLArticle").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLArticle is the interface that describes a <article> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLArticle interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Article").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=50245:50271#L1914").
								Body(
									app.Text("Article"),
								),
							app.A().
								Class("permalink").
								Href("#Article").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Article()"),
						app.A().
							Href("#HTMLArticle").
							Body(
								app.Text("HTMLArticle"),
							),
					),
					app.P().Body(
						app.Text("Article returns an HTML element that defines an article."),
					),
					app.H2().
						ID("HTMLAside").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=55465:60924#L2174").
								Body(
									app.Text("HTMLAside"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLAside").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLAside is the interface that describes a <aside> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLAside interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Aside").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=61009:61031#L2323").
								Body(
									app.Text("Aside"),
								),
							app.A().
								Class("permalink").
								Href("#Aside").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Aside()"),
						app.A().
							Href("#HTMLAside").
							Body(
								app.Text("HTMLAside"),
							),
					),
					app.P().Body(
						app.Text("Aside returns an HTML element that defines content aside from the page content."),
					),
					app.H2().
						ID("HTMLAudio").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=66027:75436#L2583").
								Body(
									app.Text("HTMLAudio"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLAudio").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLAudio is the interface that describes a <audio> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLAudio interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoPlay specifies that the audio/video will start playing as soon as it is ready."),
							),
						app.Text("AutoPlay(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Controls specifies that audio/video controls should be displayed (such as a play/pause button etc)."),
							),
						app.Text("Controls(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Loop specifies that the audio/video will start over again, every time it is finished."),
							),
						app.Text("Loop(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Muted specifies that the audio output of the video should be muted."),
							),
						app.Text("Muted(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Preload specifies if and how the author thinks the audio/video should be loaded when the page loads."),
							),
						app.Text("Preload(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnAbort calls the given handler on abort."),
							),
						app.Text("OnAbort(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlay calls the given handler when a file is ready to start playing (when it has buffered enough to begin)."),
							),
						app.Text("OnCanPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlayThrough calls the given handler when a file can be played all the way to the end without pausing for buffering."),
							),
						app.Text("OnCanPlayThrough(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCueChange calls the given handler when the cue changes in a track element."),
							),
						app.Text("OnCueChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDurationChange calls the given handler when the length of the media changes."),
							),
						app.Text("OnDurationChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEmptied calls the given handler when something bad happens and the file is suddenly unavailable (like unexpectedly disconnects)."),
							),
						app.Text("OnEmptied(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEnded calls the given handler when the media has reach the end."),
							),
						app.Text("OnEnded(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnError calls the given handler when an error occurs."),
							),
						app.Text("OnError(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadStart calls the given handler just as the file begins to load before anything is actually loaded."),
							),
						app.Text("OnLoadStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadedData calls the given handler when media data is loaded."),
							),
						app.Text("OnLoadedData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPause calls the given handler when the media is paused either by the user or programmatically."),
							),
						app.Text("OnPause(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlay calls the given handler when the media is ready to start playing."),
							),
						app.Text("OnPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlaying calls the given handler when the media actually has started playing."),
							),
						app.Text("OnPlaying(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnProgress calls the given handler when the browser is in the process of getting the media data."),
							),
						app.Text("OnProgress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnRateChange calls the given handler each time the playback rate changes (like when a user switches to a slow motion or fast forward mode)."),
							),
						app.Text("OnRateChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeked calls the given handler when the seeking attribute is set to false indicating that seeking has ended."),
							),
						app.Text("OnSeeked(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeking calls the given handler when the seeking attribute is set to true indicating that seeking is active."),
							),
						app.Text("OnSeeking(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnStalled calls the given handler when the browser is unable to fetch the media data for whatever reason."),
							),
						app.Text("OnStalled(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSuspend calls the given handler when fetching the media data is stopped before it is completely loaded for whatever reason."),
							),
						app.Text("OnSuspend(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnTimeUpdate calls the given handler when the playing position has changed (like when the user fast forwards to a different point in the media)."),
							),
						app.Text("OnTimeUpdate(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnVolumeChange calls the given handler each time the volume is changed which (includes setting the volume to \"mute\")."),
							),
						app.Text("OnVolumeChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWaiting calls the given handler when the media has paused but is expected to resume (like when the media pauses to buffer more data)."),
							),
						app.Text("OnWaiting(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnloadedMetaData calls the given handler when meta data (like dimensions and duration) are loaded."),
							),
						app.Text("OnloadedMetaData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Audio").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=75499:75521#L2819").
								Body(
									app.Text("Audio"),
								),
							app.A().
								Class("permalink").
								Href("#Audio").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Audio()"),
						app.A().
							Href("#HTMLAudio").
							Body(
								app.Text("HTMLAudio"),
							),
					),
					app.P().Body(
						app.Text("Audio returns an HTML element that defines sound content."),
					),
					app.H2().
						ID("HTMLB").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=83479:88742#L3224").
								Body(
									app.Text("HTMLB"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLB").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLB is the interface that describes a <b> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLB interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("B").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=88797:88811#L3373").
								Body(
									app.Text("B"),
								),
							app.A().
								Class("permalink").
								Href("#B").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func B()"),
						app.A().
							Href("#HTMLB").
							Body(
								app.Text("HTMLB"),
							),
					),
					app.P().Body(
						app.Text("B returns an HTML element that defines bold text."),
					),
					app.H2().
						ID("HTMLBase").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=93409:98830#L3633").
								Body(
									app.Text("HTMLBase"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLBase").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLBase is the interface that describes a <base> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLBase interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Href specifies the URL of the page the link goes to."),
							),
						app.Text("Href(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Target specifies the target for where to open the linked document or where to submit the form."),
							),
						app.Text("Target(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Base").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=98936:98956#L3782").
								Body(
									app.Text("Base"),
								),
							app.A().
								Class("permalink").
								Href("#Base").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Base()"),
						app.A().
							Href("#HTMLBase").
							Body(
								app.Text("HTMLBase"),
							),
					),
					app.P().Body(
						app.Text("Base returns an HTML element that specifies the base URL/target for all relative URLs in a document."),
					),
					app.H2().
						ID("HTMLBdi").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=103869:109230#L4043").
								Body(
									app.Text("HTMLBdi"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLBdi").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLBdi is the interface that describes a <bdi> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLBdi interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Bdi").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=109369:109387#L4192").
								Body(
									app.Text("Bdi"),
								),
							app.A().
								Class("permalink").
								Href("#Bdi").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Bdi()"),
						app.A().
							Href("#HTMLBdi").
							Body(
								app.Text("HTMLBdi"),
							),
					),
					app.P().Body(
						app.Text("Bdi returns an HTML element that isolates a part of text that might be formatted in a different direction from other text outside it."),
					),
					app.H2().
						ID("HTMLBdo").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=114181:119542#L4452").
								Body(
									app.Text("HTMLBdo"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLBdo").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLBdo is the interface that describes a <bdo> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLBdo interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Bdo").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=119618:119636#L4601").
								Body(
									app.Text("Bdo"),
								),
							app.A().
								Class("permalink").
								Href("#Bdo").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Bdo()"),
						app.A().
							Href("#HTMLBdo").
							Body(
								app.Text("HTMLBdo"),
							),
					),
					app.P().Body(
						app.Text("Bdo returns an HTML element that overrides the current text direction."),
					),
					app.H2().
						ID("HTMLBlockquote").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=124444:130253#L4861").
								Body(
									app.Text("HTMLBlockquote"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLBlockquote").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLBlockquote is the interface that describes a <blockquote> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLBlockquote interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Cite specifies a URL which explains the quote/deleted/inserted text."),
							),
						app.Text("Cite(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Blockquote").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=130352:130384#L5013").
								Body(
									app.Text("Blockquote"),
								),
							app.A().
								Class("permalink").
								Href("#Blockquote").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Blockquote()"),
						app.A().
							Href("#HTMLBlockquote").
							Body(
								app.Text("HTMLBlockquote"),
							),
					),
					app.P().Body(
						app.Text("Blockquote returns an HTML element that defines a section that is quoted from another source."),
					),
					app.H2().
						ID("HTMLBody").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=135981:143120#L5278").
								Body(
									app.Text("HTMLBody"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLBody").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLBody is the interface that describes a <body> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLBody interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnAfterPrint runs the given handler after the document is printed."),
							),
						app.Text("OnAfterPrint(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBeforePrint calls the given handler before the document is printed."),
							),
						app.Text("OnBeforePrint(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBeforeUnload calls the given handler when the document is about to be unloaded."),
							),
						app.Text("OnBeforeUnload(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnError calls the given handler when an error occurs."),
							),
						app.Text("OnError(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnHashChange calls the given handler when there has been changes to the anchor part of the a URL."),
							),
						app.Text("OnHashChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoad calls the given handler after the element is finished loading."),
							),
						app.Text("OnLoad(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMessage calls then given handler when a message is triggered."),
							),
						app.Text("OnMessage(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnOffline calls the given handler when the browser starts to work offline."),
							),
						app.Text("OnOffline(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnOnline calls the given handler when the browser starts to work online."),
							),
						app.Text("OnOnline(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPageHide calls the given handler when a user navigates away from a page."),
							),
						app.Text("OnPageHide(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPageShow calls the given handler when a user navigates to a page."),
							),
						app.Text("OnPageShow(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPopState calls the given handler when the window's history changes."),
							),
						app.Text("OnPopState(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnResize calls the given handler when the browser window is resized."),
							),
						app.Text("OnResize(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnStorage calls the given handler when a Web Storage area is updated."),
							),
						app.Text("OnStorage(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnUnload calls the given handler once a page has unloaded (or the browser window has been closed)."),
							),
						app.Text("OnUnload(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Body").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=143188:143208#L5472").
								Body(
									app.Text("Body"),
								),
							app.A().
								Class("permalink").
								Href("#Body").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Body()"),
						app.A().
							Href("#HTMLBody").
							Body(
								app.Text("HTMLBody"),
							),
					),
					app.P().Body(
						app.Text("Body returns an HTML element that defines the document's body."),
					),
					app.H2().
						ID("HTMLBr").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=149649:154766#L5807").
								Body(
									app.Text("HTMLBr"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLBr").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLBr is the interface that describes a <br> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLBr interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Br").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=154832:154848#L5950").
								Body(
									app.Text("Br"),
								),
							app.A().
								Class("permalink").
								Href("#Br").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Br()"),
						app.A().
							Href("#HTMLBr").
							Body(
								app.Text("HTMLBr"),
							),
					),
					app.P().Body(
						app.Text("Br returns an HTML element that defines a single line break."),
					),
					app.H2().
						ID("HTMLButton").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=159397:166158#L6201").
								Body(
									app.Text("HTMLButton"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLButton").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLButton is the interface that describes a <button> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLButton interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoFocus specifies that the element should automatically get focus when the page loads."),
							),
						app.Text("AutoFocus(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies that the specified element/group of elements should be disabled."),
							),
						app.Text("Disabled(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormAction specifies where to send the form-data when a form is submitted. Only for submit type."),
							),
						app.Text("FormAction(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormEncType specifies how form-data should be encoded before sending it to a server. Only for submit type."),
							),
						app.Text("FormEncType(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormMethod specifies how to send the form-data (which HTTP method to use). Only for submit type."),
							),
						app.Text("FormMethod(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormNoValidate specifies that the form-data should not be validated on submission. Only for submit type."),
							),
						app.Text("FormNoValidate(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormTarget specifies where to display the response after submitting the form. Only for submit type."),
							),
						app.Text("FormTarget(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Button").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=166227:166251#L6383").
								Body(
									app.Text("Button"),
								),
							app.A().
								Class("permalink").
								Href("#Button").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Button()"),
						app.A().
							Href("#HTMLButton").
							Body(
								app.Text("HTMLButton"),
							),
					),
					app.P().Body(
						app.Text("Button returns an HTML element that defines a clickable button."),
					),
					app.H2().
						ID("HTMLCanvas").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=172361:178028#L6698").
								Body(
									app.Text("HTMLCanvas"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLCanvas").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLCanvas is the interface that describes a <canvas> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLCanvas interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Height specifies the height of the element (in pixels)."),
							),
						app.Text("Height(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Width specifies the width of the element."),
							),
						app.Text("Width(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Canvas").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=178106:178130#L6853").
								Body(
									app.Text("Canvas"),
								),
							app.A().
								Class("permalink").
								Href("#Canvas").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Canvas()"),
						app.A().
							Href("#HTMLCanvas").
							Body(
								app.Text("HTMLCanvas"),
							),
					),
					app.P().Body(
						app.Text("Canvas returns an HTML element that is used to draw graphics on the fly."),
					),
					app.H2().
						ID("HTMLCaption").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=183397:188954#L7123").
								Body(
									app.Text("HTMLCaption"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLCaption").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLCaption is the interface that describes a <caption> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLCaption interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Caption").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=189021:189047#L7272").
								Body(
									app.Text("Caption"),
								),
							app.A().
								Class("permalink").
								Href("#Caption").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Caption()"),
						app.A().
							Href("#HTMLCaption").
							Body(
								app.Text("HTMLCaption"),
							),
					),
					app.P().Body(
						app.Text("Caption returns an HTML element that defines a table caption."),
					),
					app.H2().
						ID("HTMLCite").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=194239:199649#L7532").
								Body(
									app.Text("HTMLCite"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLCite").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLCite is the interface that describes a <cite> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLCite interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Cite").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=199717:199737#L7681").
								Body(
									app.Text("Cite"),
								),
							app.A().
								Class("permalink").
								Href("#Cite").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Cite()"),
						app.A().
							Href("#HTMLCite").
							Body(
								app.Text("HTMLCite"),
							),
					),
					app.P().Body(
						app.Text("Cite returns an HTML element that defines the title of a work."),
					),
					app.H2().
						ID("HTMLCode").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=204632:210042#L7941").
								Body(
									app.Text("HTMLCode"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLCode").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLCode is the interface that describes a <code> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLCode interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Code").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=210115:210135#L8090").
								Body(
									app.Text("Code"),
								),
							app.A().
								Class("permalink").
								Href("#Code").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Code()"),
						app.A().
							Href("#HTMLCode").
							Body(
								app.Text("HTMLCode"),
							),
					),
					app.P().Body(
						app.Text("Code returns an HTML element that defines a piece of computer code."),
					),
					app.H2().
						ID("HTMLCol").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=215028:220264#L8350").
								Body(
									app.Text("HTMLCol"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLCol").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLCol is the interface that describes a <col> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLCol interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Span specifies the number of columns to span."),
							),
						app.Text("Span(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Col").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=220373:220391#L8496").
								Body(
									app.Text("Col"),
								),
							app.A().
								Class("permalink").
								Href("#Col").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Col()"),
						app.A().
							Href("#HTMLCol").
							Body(
								app.Text("HTMLCol"),
							),
					),
					app.P().Body(
						app.Text("Col returns an HTML element that specifies column properties for each column within a colgroup element."),
					),
					app.H2().
						ID("HTMLColGroup").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=225114:230797#L8752").
								Body(
									app.Text("HTMLColGroup"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLColGroup").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLColGroup is the interface that describes a <colgroup> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLColGroup interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Span specifies the number of columns to span."),
							),
						app.Text("Span(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("ColGroup").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=230908:230936#L8904").
								Body(
									app.Text("ColGroup"),
								),
							app.A().
								Class("permalink").
								Href("#ColGroup").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func ColGroup()"),
						app.A().
							Href("#HTMLColGroup").
							Body(
								app.Text("HTMLColGroup"),
							),
					),
					app.P().Body(
						app.Text("ColGroup returns an HTML element that specifies a group of one or more columns in a table for formatting."),
					),
					app.H2().
						ID("HTMLData").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=236312:238090#L9169").
								Body(
									app.Text("HTMLData"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLData").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLData is the interface that describes a <data> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLData interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Data").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=238190:238210#L9225").
								Body(
									app.Text("Data"),
								),
							app.A().
								Class("permalink").
								Href("#Data").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Data()"),
						app.A().
							Href("#HTMLData").
							Body(
								app.Text("HTMLData"),
							),
					),
					app.P().Body(
						app.Text("Data returns an HTML element that links the given content with a machine-readable translation."),
					),
					app.H2().
						ID("HTMLDataList").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=239966:245572#L9330").
								Body(
									app.Text("HTMLDataList"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDataList").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDataList is the interface that describes a <datalist> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDataList interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("DataList").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=245675:245703#L9479").
								Body(
									app.Text("DataList"),
								),
							app.A().
								Class("permalink").
								Href("#DataList").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func DataList()"),
						app.A().
							Href("#HTMLDataList").
							Body(
								app.Text("HTMLDataList"),
							),
					),
					app.P().Body(
						app.Text("DataList returns an HTML element that specifies a list of pre-defined options for input controls."),
					),
					app.H2().
						ID("HTMLDd").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=250990:256302#L9739").
								Body(
									app.Text("HTMLDd"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDd").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDd is the interface that describes a <dd> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDd interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Dd").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=256400:256416#L9888").
								Body(
									app.Text("Dd"),
								),
							app.A().
								Class("permalink").
								Href("#Dd").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Dd()"),
						app.A().
							Href("#HTMLDd").
							Body(
								app.Text("HTMLDd"),
							),
					),
					app.P().Body(
						app.Text("Dd returns an HTML element that defines a description/value of a term in a description list."),
					),
					app.H2().
						ID("HTMLDel").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=261111:266641#L10148").
								Body(
									app.Text("HTMLDel"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDel").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDel is the interface that describes a <del> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDel interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Cite specifies a URL which explains the quote/deleted/inserted text."),
							),
						app.Text("Cite(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DateTime specifies the date and time."),
							),
						app.Text("DateTime(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Del").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=266731:266749#L10303").
								Body(
									app.Text("Del"),
								),
							app.A().
								Class("permalink").
								Href("#Del").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Del()"),
						app.A().
							Href("#HTMLDel").
							Body(
								app.Text("HTMLDel"),
							),
					),
					app.P().Body(
						app.Text("Del returns an HTML element that defines text that has been deleted from a document."),
					),
					app.H2().
						ID("HTMLDetails").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=271731:277516#L10573").
								Body(
									app.Text("HTMLDetails"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDetails").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDetails is the interface that describes a <details> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDetails interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Open specifies that the details should be visible (open) to the user."),
							),
						app.Text("Open(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnToggle calls the given handler when the user opens or closes the details element."),
							),
						app.Text("OnToggle(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Details").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=277617:277643#L10728").
								Body(
									app.Text("Details"),
								),
							app.A().
								Class("permalink").
								Href("#Details").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Details()"),
						app.A().
							Href("#HTMLDetails").
							Body(
								app.Text("HTMLDetails"),
							),
					),
					app.P().Body(
						app.Text("Details returns an HTML element that defines additional details that the user can view or hide."),
					),
					app.H2().
						ID("HTMLDfn").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=283023:288384#L10998").
								Body(
									app.Text("HTMLDfn"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDfn").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDfn is the interface that describes a <dfn> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDfn interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Dfn").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=288466:288484#L11147").
								Body(
									app.Text("Dfn"),
								),
							app.A().
								Class("permalink").
								Href("#Dfn").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Dfn()"),
						app.A().
							Href("#HTMLDfn").
							Body(
								app.Text("HTMLDfn"),
							),
					),
					app.P().Body(
						app.Text("Dfn returns an HTML element that represents the defining instance of a term."),
					),
					app.H2().
						ID("HTMLDialog").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=293284:298892#L11407").
								Body(
									app.Text("HTMLDialog"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDialog").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDialog is the interface that describes a <dialog> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDialog interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Open specifies that the details should be visible (open) to the user."),
							),
						app.Text("Open(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Dialog").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=298965:298989#L11559").
								Body(
									app.Text("Dialog"),
								),
							app.A().
								Class("permalink").
								Href("#Dialog").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Dialog()"),
						app.A().
							Href("#HTMLDialog").
							Body(
								app.Text("HTMLDialog"),
							),
					),
					app.P().Body(
						app.Text("Dialog returns an HTML element that defines a dialog box or window."),
					),
					app.H2().
						ID("HTMLDiv").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=304162:309523#L11824").
								Body(
									app.Text("HTMLDiv"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDiv").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDiv is the interface that describes a <div> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDiv interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Div").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=309594:309612#L11973").
								Body(
									app.Text("Div"),
								),
							app.A().
								Class("permalink").
								Href("#Div").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Div()"),
						app.A().
							Href("#HTMLDiv").
							Body(
								app.Text("HTMLDiv"),
							),
					),
					app.P().Body(
						app.Text("Div returns an HTML element that defines a section in a document."),
					),
					app.H2().
						ID("HTMLDl").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=314404:319716#L12233").
								Body(
									app.Text("HTMLDl"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDl").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDl is the interface that describes a <dl> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDl interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Dl").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=319781:319797#L12382").
								Body(
									app.Text("Dl"),
								),
							app.A().
								Class("permalink").
								Href("#Dl").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Dl()"),
						app.A().
							Href("#HTMLDl").
							Body(
								app.Text("HTMLDl"),
							),
					),
					app.P().Body(
						app.Text("Dl returns an HTML element that defines a description list."),
					),
					app.H2().
						ID("HTMLDt").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=324490:329802#L12642").
								Body(
									app.Text("HTMLDt"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLDt").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLDt is the interface that describes a <dt> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLDt interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Dt").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=329882:329898#L12791").
								Body(
									app.Text("Dt"),
								),
							app.A().
								Class("permalink").
								Href("#Dt").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Dt()"),
						app.A().
							Href("#HTMLDt").
							Body(
								app.Text("HTMLDt"),
							),
					),
					app.P().Body(
						app.Text("Dt returns an HTML element that defines a term/name in a description list."),
					),
					app.H2().
						ID("HTMLEm").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=334591:339903#L13051").
								Body(
									app.Text("HTMLEm"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLEm").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLEm is the interface that describes a <em> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLEm interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Em").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=339965:339981#L13200").
								Body(
									app.Text("Em"),
								),
							app.A().
								Class("permalink").
								Href("#Em").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Em()"),
						app.A().
							Href("#HTMLEm").
							Body(
								app.Text("HTMLEm"),
							),
					),
					app.P().Body(
						app.Text("Em returns an HTML element that defines emphasized text."),
					),
					app.H2().
						ID("HTMLEmbed").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=344680:353515#L13460").
								Body(
									app.Text("HTMLEmbed"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLEmbed").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLEmbed is the interface that describes a <embed> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLEmbed interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Height specifies the height of the element (in pixels)."),
							),
						app.Text("Height(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Width specifies the width of the element."),
							),
						app.Text("Width(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnAbort calls the given handler on abort."),
							),
						app.Text("OnAbort(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlay calls the given handler when a file is ready to start playing (when it has buffered enough to begin)."),
							),
						app.Text("OnCanPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlayThrough calls the given handler when a file can be played all the way to the end without pausing for buffering."),
							),
						app.Text("OnCanPlayThrough(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCueChange calls the given handler when the cue changes in a track element."),
							),
						app.Text("OnCueChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDurationChange calls the given handler when the length of the media changes."),
							),
						app.Text("OnDurationChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEmptied calls the given handler when something bad happens and the file is suddenly unavailable (like unexpectedly disconnects)."),
							),
						app.Text("OnEmptied(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEnded calls the given handler when the media has reach the end."),
							),
						app.Text("OnEnded(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnError calls the given handler when an error occurs."),
							),
						app.Text("OnError(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadStart calls the given handler just as the file begins to load before anything is actually loaded."),
							),
						app.Text("OnLoadStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadedData calls the given handler when media data is loaded."),
							),
						app.Text("OnLoadedData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPause calls the given handler when the media is paused either by the user or programmatically."),
							),
						app.Text("OnPause(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlay calls the given handler when the media is ready to start playing."),
							),
						app.Text("OnPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlaying calls the given handler when the media actually has started playing."),
							),
						app.Text("OnPlaying(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnProgress calls the given handler when the browser is in the process of getting the media data."),
							),
						app.Text("OnProgress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnRateChange calls the given handler each time the playback rate changes (like when a user switches to a slow motion or fast forward mode)."),
							),
						app.Text("OnRateChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeked calls the given handler when the seeking attribute is set to false indicating that seeking has ended."),
							),
						app.Text("OnSeeked(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeking calls the given handler when the seeking attribute is set to true indicating that seeking is active."),
							),
						app.Text("OnSeeking(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnStalled calls the given handler when the browser is unable to fetch the media data for whatever reason."),
							),
						app.Text("OnStalled(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSuspend calls the given handler when fetching the media data is stopped before it is completely loaded for whatever reason."),
							),
						app.Text("OnSuspend(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnTimeUpdate calls the given handler when the playing position has changed (like when the user fast forwards to a different point in the media)."),
							),
						app.Text("OnTimeUpdate(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnVolumeChange calls the given handler each time the volume is changed which (includes setting the volume to \"mute\")."),
							),
						app.Text("OnVolumeChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWaiting calls the given handler when the media has paused but is expected to resume (like when the media pauses to buffer more data)."),
							),
						app.Text("OnWaiting(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnloadedMetaData calls the given handler when meta data (like dimensions and duration) are loaded."),
							),
						app.Text("OnloadedMetaData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Embed").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=353615:353637#L13684").
								Body(
									app.Text("Embed"),
								),
							app.A().
								Class("permalink").
								Href("#Embed").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Embed()"),
						app.A().
							Href("#HTMLEmbed").
							Body(
								app.Text("HTMLEmbed"),
							),
					),
					app.P().Body(
						app.Text("Embed returns an HTML element that defines a container for an external (non-HTML) application."),
					),
					app.H2().
						ID("HTMLFieldSet").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=361265:367159#L14070").
								Body(
									app.Text("HTMLFieldSet"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLFieldSet").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLFieldSet is the interface that describes a <fieldset> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLFieldSet interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies that the specified element/group of elements should be disabled."),
							),
						app.Text("Disabled(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("FieldSet").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=367237:367265#L14228").
								Body(
									app.Text("FieldSet"),
								),
							app.A().
								Class("permalink").
								Href("#FieldSet").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func FieldSet()"),
						app.A().
							Href("#HTMLFieldSet").
							Body(
								app.Text("HTMLFieldSet"),
							),
					),
					app.P().Body(
						app.Text("FieldSet returns an HTML element that groups related elements in a form."),
					),
					app.H2().
						ID("HTMLFigCaption").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=372838:378542#L14503").
								Body(
									app.Text("HTMLFigCaption"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLFigCaption").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLFigCaption is the interface that describes a <figcaption> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLFigCaption interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("FigCaption").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=378627:378659#L14652").
								Body(
									app.Text("FigCaption"),
								),
							app.A().
								Class("permalink").
								Href("#FigCaption").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func FigCaption()"),
						app.A().
							Href("#HTMLFigCaption").
							Body(
								app.Text("HTMLFigCaption"),
							),
					),
					app.P().Body(
						app.Text("FigCaption returns an HTML element that defines a caption for a figure element."),
					),
					app.H2().
						ID("HTMLFigure").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=384152:389660#L14912").
								Body(
									app.Text("HTMLFigure"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLFigure").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLFigure is the interface that describes a <figure> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLFigure interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Figure").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=389735:389759#L15061").
								Body(
									app.Text("Figure"),
								),
							app.A().
								Class("permalink").
								Href("#Figure").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Figure()"),
						app.A().
							Href("#HTMLFigure").
							Body(
								app.Text("HTMLFigure"),
							),
					),
					app.P().Body(
						app.Text("Figure returns an HTML element that specifies self-contained content."),
					),
					app.H2().
						ID("HTMLFooter").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=394856:400364#L15321").
								Body(
									app.Text("HTMLFooter"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLFooter").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLFooter is the interface that describes a <footer> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLFooter interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Footer").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=400449:400473#L15470").
								Body(
									app.Text("Footer"),
								),
							app.A().
								Class("permalink").
								Href("#Footer").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Footer()"),
						app.A().
							Href("#HTMLFooter").
							Body(
								app.Text("HTMLFooter"),
							),
					),
					app.P().Body(
						app.Text("Footer returns an HTML element that defines a footer for a document or section."),
					),
					app.H2().
						ID("HTMLForm").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=405566:411870#L15730").
								Body(
									app.Text("HTMLForm"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLForm").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLForm is the interface that describes a <form> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLForm interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AcceptCharset specifies the character encodings that are to be used for the form submission."),
							),
						app.Text("AcceptCharset(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Action specifies where to send the form-data when a form is submitted."),
							),
						app.Text("Action(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoComplete specifies whether the element should have autocomplete enabled."),
							),
						app.Text("AutoComplete(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// EncType specifies how the form-data should be encoded when submitting it to the server (only for post method)."),
							),
						app.Text("EncType(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Method specifies the HTTP method to use when sending form-data."),
							),
						app.Text("Method(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// NoValidate specifies that the form should not be validated when submitted."),
							),
						app.Text("NoValidate(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Target specifies the target for where to open the linked document or where to submit the form."),
							),
						app.Text("Target(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Form").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=411946:411966#L15903").
								Body(
									app.Text("Form"),
								),
							app.A().
								Class("permalink").
								Href("#Form").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Form()"),
						app.A().
							Href("#HTMLForm").
							Body(
								app.Text("HTMLForm"),
							),
					),
					app.P().Body(
						app.Text("Form returns an HTML element that defines an HTML form for user input."),
					),
					app.H2().
						ID("HTMLH1").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=417592:422904#L16208").
								Body(
									app.Text("HTMLH1"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLH1").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLH1 is the interface that describes a <h1> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLH1 interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("H1").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=422963:422979#L16357").
								Body(
									app.Text("H1"),
								),
							app.A().
								Class("permalink").
								Href("#H1").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func H1()"),
						app.A().
							Href("#HTMLH1").
							Body(
								app.Text("HTMLH1"),
							),
					),
					app.P().Body(
						app.Text("H1 returns an HTML element that defines HTML heading."),
					),
					app.H2().
						ID("HTMLH2").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=427672:432984#L16617").
								Body(
									app.Text("HTMLH2"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLH2").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLH2 is the interface that describes a <h2> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLH2 interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("H2").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=433043:433059#L16766").
								Body(
									app.Text("H2"),
								),
							app.A().
								Class("permalink").
								Href("#H2").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func H2()"),
						app.A().
							Href("#HTMLH2").
							Body(
								app.Text("HTMLH2"),
							),
					),
					app.P().Body(
						app.Text("H2 returns an HTML element that defines HTML heading."),
					),
					app.H2().
						ID("HTMLH3").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=437752:443064#L17026").
								Body(
									app.Text("HTMLH3"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLH3").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLH3 is the interface that describes a <h3> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLH3 interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("H3").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=443123:443139#L17175").
								Body(
									app.Text("H3"),
								),
							app.A().
								Class("permalink").
								Href("#H3").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func H3()"),
						app.A().
							Href("#HTMLH3").
							Body(
								app.Text("HTMLH3"),
							),
					),
					app.P().Body(
						app.Text("H3 returns an HTML element that defines HTML heading."),
					),
					app.H2().
						ID("HTMLH4").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=447832:453144#L17435").
								Body(
									app.Text("HTMLH4"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLH4").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLH4 is the interface that describes a <h4> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLH4 interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("H4").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=453203:453219#L17584").
								Body(
									app.Text("H4"),
								),
							app.A().
								Class("permalink").
								Href("#H4").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func H4()"),
						app.A().
							Href("#HTMLH4").
							Body(
								app.Text("HTMLH4"),
							),
					),
					app.P().Body(
						app.Text("H4 returns an HTML element that defines HTML heading."),
					),
					app.H2().
						ID("HTMLH5").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=457912:463224#L17844").
								Body(
									app.Text("HTMLH5"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLH5").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLH5 is the interface that describes a <h5> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLH5 interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("H5").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=463283:463299#L17993").
								Body(
									app.Text("H5"),
								),
							app.A().
								Class("permalink").
								Href("#H5").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func H5()"),
						app.A().
							Href("#HTMLH5").
							Body(
								app.Text("HTMLH5"),
							),
					),
					app.P().Body(
						app.Text("H5 returns an HTML element that defines HTML heading."),
					),
					app.H2().
						ID("HTMLH6").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=467992:473304#L18253").
								Body(
									app.Text("HTMLH6"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLH6").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLH6 is the interface that describes a <h6> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLH6 interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("H6").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=473363:473379#L18402").
								Body(
									app.Text("H6"),
								),
							app.A().
								Class("permalink").
								Href("#H6").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func H6()"),
						app.A().
							Href("#HTMLH6").
							Body(
								app.Text("HTMLH6"),
							),
					),
					app.P().Body(
						app.Text("H6 returns an HTML element that defines HTML heading."),
					),
					app.H2().
						ID("HTMLHead").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=478076:479776#L18662").
								Body(
									app.Text("HTMLHead"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLHead").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLHead is the interface that describes a <head> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLHead interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Head").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=479855:479875#L18715").
								Body(
									app.Text("Head"),
								),
							app.A().
								Class("permalink").
								Href("#Head").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Head()"),
						app.A().
							Href("#HTMLHead").
							Body(
								app.Text("HTMLHead"),
							),
					),
					app.P().Body(
						app.Text("Head returns an HTML element that defines information about the document."),
					),
					app.H2().
						ID("HTMLHeader").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=481540:487048#L18815").
								Body(
									app.Text("HTMLHeader"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLHeader").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLHeader is the interface that describes a <header> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLHeader interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Header").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=487133:487157#L18964").
								Body(
									app.Text("Header"),
								),
							app.A().
								Class("permalink").
								Href("#Header").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Header()"),
						app.A().
							Href("#HTMLHeader").
							Body(
								app.Text("HTMLHeader"),
							),
					),
					app.P().Body(
						app.Text("Header returns an HTML element that defines a header for a document or section."),
					),
					app.H2().
						ID("HTMLHr").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=492246:497363#L19224").
								Body(
									app.Text("HTMLHr"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLHr").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLHr is the interface that describes a <hr> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLHr interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Hr").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=497442:497458#L19367").
								Body(
									app.Text("Hr"),
								),
							app.A().
								Class("permalink").
								Href("#Hr").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Hr()"),
						app.A().
							Href("#HTMLHr").
							Body(
								app.Text("HTMLHr"),
							),
					),
					app.P().Body(
						app.Text("Hr returns an HTML element that defines a thematic change in the content."),
					),
					app.H2().
						ID("HTMLHtml").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=502003:503703#L19618").
								Body(
									app.Text("HTMLHtml"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLHtml").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLHtml is the interface that describes a <html> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLHtml interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Html").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=503780:503800#L19671").
								Body(
									app.Text("Html"),
								),
							app.A().
								Class("permalink").
								Href("#Html").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Html()"),
						app.A().
							Href("#HTMLHtml").
							Body(
								app.Text("HTMLHtml"),
							),
					),
					app.P().Body(
						app.Text("Html returns an HTML element that defines the root of an HTML document."),
					),
					app.H2().
						ID("HTMLI").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=505455:510718#L19771").
								Body(
									app.Text("HTMLI"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLI").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLI is the interface that describes a <i> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLI interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("I").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=510808:510822#L19920").
								Body(
									app.Text("I"),
								),
							app.A().
								Class("permalink").
								Href("#I").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func I()"),
						app.A().
							Href("#HTMLI").
							Body(
								app.Text("HTMLI"),
							),
					),
					app.P().Body(
						app.Text("I returns an HTML element that defines a part of text in an alternate voice or mood."),
					),
					app.H2().
						ID("HTMLIFrame").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=515424:521978#L20180").
								Body(
									app.Text("HTMLIFrame"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLIFrame").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLIFrame is the interface that describes a <iframe> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLIFrame interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AllowFullscreen reports whether an iframe can activate fullscreen mode."),
							),
						app.Text("AllowFullscreen(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AllowPaymentRequest reports whether an iframe should be allowed to invoke the Payment Request API"),
							),
						app.Text("AllowPaymentRequest(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Height specifies the height of the element (in pixels)."),
							),
						app.Text("Height(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ReferrerPolicy specifies how much/which referrer information that will be sent when processing the iframe attributes"),
							),
						app.Text("ReferrerPolicy(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Sandbox enables an extra set of restrictions for the content in an iframe."),
							),
						app.Text("Sandbox(v interface{})"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// SrcDoc specifies the HTML content of the page to show in the iframe."),
							),
						app.Text("SrcDoc(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Width specifies the width of the element."),
							),
						app.Text("Width(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoad calls the given handler after the element is finished loading."),
							),
						app.Text("OnLoad(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("IFrame").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=522044:522068#L20359").
								Body(
									app.Text("IFrame"),
								),
							app.A().
								Class("permalink").
								Href("#IFrame").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func IFrame()"),
						app.A().
							Href("#HTMLIFrame").
							Body(
								app.Text("HTMLIFrame"),
							),
					),
					app.P().Body(
						app.Text("IFrame returns an HTML element that defines an inline frame."),
					),
					app.H2().
						ID("HTMLImg").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=528190:537369#L20679").
								Body(
									app.Text("HTMLImg"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLImg").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLImg is the interface that describes a <img> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLImg interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Alt specifies an alternate text when the original element fails to display."),
							),
						app.Text("Alt(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Height specifies the height of the element (in pixels)."),
							),
						app.Text("Height(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// IsMap specifies an image as a server-side image-map."),
							),
						app.Text("IsMap(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Sizes specifies the size of the linked resource."),
							),
						app.Text("Sizes(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// SrcSet specifies the URL of the image to use in different situations."),
							),
						app.Text("SrcSet(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// UseMap specifies an image as a client-side image-map."),
							),
						app.Text("UseMap(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Width specifies the width of the element."),
							),
						app.Text("Width(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnAbort calls the given handler on abort."),
							),
						app.Text("OnAbort(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlay calls the given handler when a file is ready to start playing (when it has buffered enough to begin)."),
							),
						app.Text("OnCanPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlayThrough calls the given handler when a file can be played all the way to the end without pausing for buffering."),
							),
						app.Text("OnCanPlayThrough(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCueChange calls the given handler when the cue changes in a track element."),
							),
						app.Text("OnCueChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDurationChange calls the given handler when the length of the media changes."),
							),
						app.Text("OnDurationChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEmptied calls the given handler when something bad happens and the file is suddenly unavailable (like unexpectedly disconnects)."),
							),
						app.Text("OnEmptied(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEnded calls the given handler when the media has reach the end."),
							),
						app.Text("OnEnded(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnError calls the given handler when an error occurs."),
							),
						app.Text("OnError(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoad calls the given handler after the element is finished loading."),
							),
						app.Text("OnLoad(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadStart calls the given handler just as the file begins to load before anything is actually loaded."),
							),
						app.Text("OnLoadStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadedData calls the given handler when media data is loaded."),
							),
						app.Text("OnLoadedData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPause calls the given handler when the media is paused either by the user or programmatically."),
							),
						app.Text("OnPause(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlay calls the given handler when the media is ready to start playing."),
							),
						app.Text("OnPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlaying calls the given handler when the media actually has started playing."),
							),
						app.Text("OnPlaying(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnProgress calls the given handler when the browser is in the process of getting the media data."),
							),
						app.Text("OnProgress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnRateChange calls the given handler each time the playback rate changes (like when a user switches to a slow motion or fast forward mode)."),
							),
						app.Text("OnRateChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeked calls the given handler when the seeking attribute is set to false indicating that seeking has ended."),
							),
						app.Text("OnSeeked(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeking calls the given handler when the seeking attribute is set to true indicating that seeking is active."),
							),
						app.Text("OnSeeking(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnStalled calls the given handler when the browser is unable to fetch the media data for whatever reason."),
							),
						app.Text("OnStalled(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSuspend calls the given handler when fetching the media data is stopped before it is completely loaded for whatever reason."),
							),
						app.Text("OnSuspend(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnTimeUpdate calls the given handler when the playing position has changed (like when the user fast forwards to a different point in the media)."),
							),
						app.Text("OnTimeUpdate(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnVolumeChange calls the given handler each time the volume is changed which (includes setting the volume to \"mute\")."),
							),
						app.Text("OnVolumeChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWaiting calls the given handler when the media has paused but is expected to resume (like when the media pauses to buffer more data)."),
							),
						app.Text("OnWaiting(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnloadedMetaData calls the given handler when meta data (like dimensions and duration) are loaded."),
							),
						app.Text("OnloadedMetaData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Img").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=537425:537443#L20918").
								Body(
									app.Text("Img"),
								),
							app.A().
								Class("permalink").
								Href("#Img").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Img()"),
						app.A().
							Href("#HTMLImg").
							Body(
								app.Text("HTMLImg"),
							),
					),
					app.P().Body(
						app.Text("Img returns an HTML element that defines an image."),
					),
					app.H2().
						ID("HTMLInput").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=545197:553648#L21329").
								Body(
									app.Text("HTMLInput"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLInput").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLInput is the interface that describes a <input> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLInput interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Accept specifies the types of files that the server accepts (only for file type)."),
							),
						app.Text("Accept(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Alt specifies an alternate text when the original element fails to display."),
							),
						app.Text("Alt(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoComplete specifies whether the element should have autocomplete enabled."),
							),
						app.Text("AutoComplete(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoFocus specifies that the element should automatically get focus when the page loads."),
							),
						app.Text("AutoFocus(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Checked specifies that an input element should be pre-selected when the page loads (for checkbox or radio types)."),
							),
						app.Text("Checked(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DirName specifies that the text direction will be submitted."),
							),
						app.Text("DirName(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies that the specified element/group of elements should be disabled."),
							),
						app.Text("Disabled(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormAction specifies where to send the form-data when a form is submitted. Only for submit type."),
							),
						app.Text("FormAction(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormEncType specifies how form-data should be encoded before sending it to a server. Only for submit type."),
							),
						app.Text("FormEncType(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormMethod specifies how to send the form-data (which HTTP method to use). Only for submit type."),
							),
						app.Text("FormMethod(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormNoValidate specifies that the form-data should not be validated on submission. Only for submit type."),
							),
						app.Text("FormNoValidate(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// FormTarget specifies where to display the response after submitting the form. Only for submit type."),
							),
						app.Text("FormTarget(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Height specifies the height of the element (in pixels)."),
							),
						app.Text("Height(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// List refers to a datalist element that contains pre-defined options for an input element."),
							),
						app.Text("List(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Max Specifies the maximum value."),
							),
						app.Text("Max(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// MaxLength specifies the maximum number of characters allowed in an element."),
							),
						app.Text("MaxLength(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Min specifies a minimum value."),
							),
						app.Text("Min(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Multiple specifies that a user can enter more than one value."),
							),
						app.Text("Multiple(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Pattern specifies a regular expression that an input element's value is checked against."),
							),
						app.Text("Pattern(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Placeholder specifies a short hint that describes the expected value of the element."),
							),
						app.Text("Placeholder(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ReadOnly specifies that the element is read-only."),
							),
						app.Text("ReadOnly(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Required specifies that the element must be filled out before submitting the form."),
							),
						app.Text("Required(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Size specifies the width."),
							),
						app.Text("Size(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Step specifies the legal number intervals for an input field."),
							),
						app.Text("Step(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Width specifies the width of the element."),
							),
						app.Text("Width(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoad calls the given handler after the element is finished loading."),
							),
						app.Text("OnLoad(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Input").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=553714:553736#L21565").
								Body(
									app.Text("Input"),
								),
							app.A().
								Class("permalink").
								Href("#Input").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Input()"),
						app.A().
							Href("#HTMLInput").
							Body(
								app.Text("HTMLInput"),
							),
					),
					app.P().Body(
						app.Text("Input returns an HTML element that defines an input control."),
					),
					app.H2().
						ID("HTMLIns").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=561321:566682#L21976").
								Body(
									app.Text("HTMLIns"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLIns").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLIns is the interface that describes a <ins> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLIns interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Ins").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=566775:566793#L22125").
								Body(
									app.Text("Ins"),
								),
							app.A().
								Class("permalink").
								Href("#Ins").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Ins()"),
						app.A().
							Href("#HTMLIns").
							Body(
								app.Text("HTMLIns"),
							),
					),
					app.P().Body(
						app.Text("Ins returns an HTML element that defines a text that has been inserted into a document."),
					),
					app.H2().
						ID("HTMLKbd").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=571587:576948#L22385").
								Body(
									app.Text("HTMLKbd"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLKbd").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLKbd is the interface that describes a <kbd> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLKbd interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Kbd").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=577010:577028#L22534").
								Body(
									app.Text("Kbd"),
								),
							app.A().
								Class("permalink").
								Href("#Kbd").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Kbd()"),
						app.A().
							Href("#HTMLKbd").
							Body(
								app.Text("HTMLKbd"),
							),
					),
					app.P().Body(
						app.Text("Kbd returns an HTML element that defines keyboard input."),
					),
					app.H2().
						ID("HTMLLabel").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=581826:587475#L22794").
								Body(
									app.Text("HTMLLabel"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLLabel").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLLabel is the interface that describes a <label> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLLabel interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// For specifies which form element(s) a label/calculation is bound to."),
							),
						app.Text("For(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Label").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=587553:587575#L22949").
								Body(
									app.Text("Label"),
								),
							app.A().
								Class("permalink").
								Href("#Label").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Label()"),
						app.A().
							Href("#HTMLLabel").
							Body(
								app.Text("HTMLLabel"),
							),
					),
					app.P().Body(
						app.Text("Label returns an HTML element that defines a label for an input element."),
					),
					app.H2().
						ID("HTMLLegends").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=592737:598294#L23219").
								Body(
									app.Text("HTMLLegends"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLLegends").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLLegends is the interface that describes a <legends> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLLegends interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Legends").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=598378:598404#L23368").
								Body(
									app.Text("Legends"),
								),
							app.A().
								Class("permalink").
								Href("#Legends").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Legends()"),
						app.A().
							Href("#HTMLLegends").
							Body(
								app.Text("HTMLLegends"),
							),
					),
					app.P().Body(
						app.Text("Legends returns an HTML element that defines a caption for a fieldset element."),
					),
					app.H2().
						ID("HTMLLi").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=603592:608980#L23628").
								Body(
									app.Text("HTMLLi"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLLi").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLLi is the interface that describes a <li> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLLi interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Li").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=609038:609054#L23780").
								Body(
									app.Text("Li"),
								),
							app.A().
								Class("permalink").
								Href("#Li").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Li()"),
						app.A().
							Href("#HTMLLi").
							Body(
								app.Text("HTMLLi"),
							),
					),
					app.P().Body(
						app.Text("Li returns an HTML element that defines a list item."),
					),
					app.H2().
						ID("HTMLLink").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=613834:619689#L24045").
								Body(
									app.Text("HTMLLink"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLLink").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLLink is the interface that describes a <link> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLLink interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Href specifies the URL of the page the link goes to."),
							),
						app.Text("Href(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// HrefLang specifies the language of the linked document."),
							),
						app.Text("HrefLang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Media specifies what media/device the linked document is optimized for."),
							),
						app.Text("Media(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Rel specifies the relationship between the current document and the linked document."),
							),
						app.Text("Rel(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Sizes specifies the size of the linked resource."),
							),
						app.Text("Sizes(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoad calls the given handler after the element is finished loading."),
							),
						app.Text("OnLoad(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Link").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=619834:619854#L24209").
								Body(
									app.Text("Link"),
								),
							app.A().
								Class("permalink").
								Href("#Link").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Link()"),
						app.A().
							Href("#HTMLLink").
							Body(
								app.Text("HTMLLink"),
							),
					),
					app.P().Body(
						app.Text("Link returns an HTML element that defines the relationship between a document and an external resource (most used to link to style sheets)."),
					),
					app.H2().
						ID("HTMLMain").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=625191:630601#L24495").
								Body(
									app.Text("HTMLMain"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLMain").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLMain is the interface that describes a <main> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLMain interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Main").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=630682:630702#L24644").
								Body(
									app.Text("Main"),
								),
							app.A().
								Class("permalink").
								Href("#Main").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Main()"),
						app.A().
							Href("#HTMLMain").
							Body(
								app.Text("HTMLMain"),
							),
					),
					app.P().Body(
						app.Text("Main returns an HTML element that specifies the main content of a document."),
					),
					app.H2().
						ID("HTMLMap").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=635595:641025#L24904").
								Body(
									app.Text("HTMLMap"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLMap").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLMap is the interface that describes a <map> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLMap interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Map").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=641096:641114#L25056").
								Body(
									app.Text("Map"),
								),
							app.A().
								Class("permalink").
								Href("#Map").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Map()"),
						app.A().
							Href("#HTMLMap").
							Body(
								app.Text("HTMLMap"),
							),
					),
					app.P().Body(
						app.Text("Map returns an HTML element that defines a client-side image-map."),
					),
					app.H2().
						ID("HTMLMark").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=645988:651398#L25321").
								Body(
									app.Text("HTMLMark"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLMark").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLMark is the interface that describes a <mark> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLMark interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Mark").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=651470:651490#L25470").
								Body(
									app.Text("Mark"),
								),
							app.A().
								Class("permalink").
								Href("#Mark").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Mark()"),
						app.A().
							Href("#HTMLMark").
							Body(
								app.Text("HTMLMark"),
							),
					),
					app.P().Body(
						app.Text("Mark returns an HTML element that defines marked/highlighted text."),
					),
					app.H2().
						ID("HTMLMeta").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=656385:658259#L25730").
								Body(
									app.Text("HTMLMeta"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLMeta").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLMeta is the interface that describes a <meta> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLMeta interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Charset specifies the character encoding."),
							),
						app.Text("Charset(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Content gives the value associated with the http-equiv or name attribute."),
							),
						app.Text("Content(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// HTTPEquiv provides an HTTP header for the information/value of the content attribute."),
							),
						app.Text("HTTPEquiv(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Meta").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=658300:658320#L25789").
								Body(
									app.Text("Meta"),
								),
							app.A().
								Class("permalink").
								Href("#Meta").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Meta()"),
						app.A().
							Href("#HTMLMeta").
							Body(
								app.Text("HTMLMeta"),
							),
					),
					app.P().Body(
						app.Text("Meta returns an HTML element that ."),
					),
					app.H2().
						ID("HTMLMeter").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=660165:666207#L25900").
								Body(
									app.Text("HTMLMeter"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLMeter").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLMeter is the interface that describes a <meter> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLMeter interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// High specifies the range that is considered to be a high value."),
							),
						app.Text("High(v"),
						app.A().
							Href("/pkg/builtin/#float64").
							Body(
								app.Text("float64"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Low specifies the range that is considered to be a low value."),
							),
						app.Text("Low(v"),
						app.A().
							Href("/pkg/builtin/#float64").
							Body(
								app.Text("float64"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Max Specifies the maximum value."),
							),
						app.Text("Max(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Min specifies a minimum value."),
							),
						app.Text("Min(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Optimum specifies what value is the optimal value for the gauge."),
							),
						app.Text("Optimum(v"),
						app.A().
							Href("/pkg/builtin/#float64").
							Body(
								app.Text("float64"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Meter").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=666308:666330#L26070").
								Body(
									app.Text("Meter"),
								),
							app.A().
								Class("permalink").
								Href("#Meter").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Meter()"),
						app.A().
							Href("#HTMLMeter").
							Body(
								app.Text("HTMLMeter"),
							),
					),
					app.P().Body(
						app.Text("Meter returns an HTML element that defines a scalar measurement within a known range (a gauge)."),
					),
					app.H2().
						ID("HTMLNav").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=671906:677267#L26365").
								Body(
									app.Text("HTMLNav"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLNav").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLNav is the interface that describes a <nav> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLNav interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Nav").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=677331:677349#L26514").
								Body(
									app.Text("Nav"),
								),
							app.A().
								Class("permalink").
								Href("#Nav").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Nav()"),
						app.A().
							Href("#HTMLNav").
							Body(
								app.Text("HTMLNav"),
							),
					),
					app.P().Body(
						app.Text("Nav returns an HTML element that defines navigation links."),
					),
					app.H2().
						ID("HTMLNoScript").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=682153:683921#L26774").
								Body(
									app.Text("HTMLNoScript"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLNoScript").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLNoScript is the interface that describes a <noscript> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLNoScript interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("NoScript").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=684044:684072#L26827").
								Body(
									app.Text("NoScript"),
								),
							app.A().
								Class("permalink").
								Href("#NoScript").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func NoScript()"),
						app.A().
							Href("#HTMLNoScript").
							Body(
								app.Text("HTMLNoScript"),
							),
					),
					app.P().Body(
						app.Text("NoScript returns an HTML element that defines an alternate content for users that do not support client-side scripts."),
					),
					app.H2().
						ID("HTMLObject").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=685877:695266#L26927").
								Body(
									app.Text("HTMLObject"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLObject").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLObject is the interface that describes a <object> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLObject interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Data specifies the URL of the resource to be used by the object."),
							),
						app.Text("Data(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Height specifies the height of the element (in pixels)."),
							),
						app.Text("Height(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// UseMap specifies an image as a client-side image-map."),
							),
						app.Text("UseMap(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Width specifies the width of the element."),
							),
						app.Text("Width(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnAbort calls the given handler on abort."),
							),
						app.Text("OnAbort(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlay calls the given handler when a file is ready to start playing (when it has buffered enough to begin)."),
							),
						app.Text("OnCanPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlayThrough calls the given handler when a file can be played all the way to the end without pausing for buffering."),
							),
						app.Text("OnCanPlayThrough(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCueChange calls the given handler when the cue changes in a track element."),
							),
						app.Text("OnCueChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDurationChange calls the given handler when the length of the media changes."),
							),
						app.Text("OnDurationChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEmptied calls the given handler when something bad happens and the file is suddenly unavailable (like unexpectedly disconnects)."),
							),
						app.Text("OnEmptied(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEnded calls the given handler when the media has reach the end."),
							),
						app.Text("OnEnded(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnError calls the given handler when an error occurs."),
							),
						app.Text("OnError(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadStart calls the given handler just as the file begins to load before anything is actually loaded."),
							),
						app.Text("OnLoadStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadedData calls the given handler when media data is loaded."),
							),
						app.Text("OnLoadedData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPause calls the given handler when the media is paused either by the user or programmatically."),
							),
						app.Text("OnPause(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlay calls the given handler when the media is ready to start playing."),
							),
						app.Text("OnPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlaying calls the given handler when the media actually has started playing."),
							),
						app.Text("OnPlaying(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnProgress calls the given handler when the browser is in the process of getting the media data."),
							),
						app.Text("OnProgress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnRateChange calls the given handler each time the playback rate changes (like when a user switches to a slow motion or fast forward mode)."),
							),
						app.Text("OnRateChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeked calls the given handler when the seeking attribute is set to false indicating that seeking has ended."),
							),
						app.Text("OnSeeked(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeking calls the given handler when the seeking attribute is set to true indicating that seeking is active."),
							),
						app.Text("OnSeeking(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnStalled calls the given handler when the browser is unable to fetch the media data for whatever reason."),
							),
						app.Text("OnStalled(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSuspend calls the given handler when fetching the media data is stopped before it is completely loaded for whatever reason."),
							),
						app.Text("OnSuspend(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnTimeUpdate calls the given handler when the playing position has changed (like when the user fast forwards to a different point in the media)."),
							),
						app.Text("OnTimeUpdate(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnVolumeChange calls the given handler each time the volume is changed which (includes setting the volume to \"mute\")."),
							),
						app.Text("OnVolumeChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWaiting calls the given handler when the media has paused but is expected to resume (like when the media pauses to buffer more data)."),
							),
						app.Text("OnWaiting(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnloadedMetaData calls the given handler when meta data (like dimensions and duration) are loaded."),
							),
						app.Text("OnloadedMetaData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Object").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=695335:695359#L27166").
								Body(
									app.Text("Object"),
								),
							app.A().
								Class("permalink").
								Href("#Object").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Object()"),
						app.A().
							Href("#HTMLObject").
							Body(
								app.Text("HTMLObject"),
							),
					),
					app.P().Body(
						app.Text("Object returns an HTML element that defines an embedded object."),
					),
					app.H2().
						ID("HTMLOl").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=703550:709107#L27576").
								Body(
									app.Text("HTMLOl"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLOl").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLOl is the interface that describes a <ol> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLOl interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Reversed specifies that the list order should be descending (9,8,7...)."),
							),
						app.Text("Reversed(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Start specifies the start value of the ordered list."),
							),
						app.Text("Start(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Ol").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=709169:709185#L27734").
								Body(
									app.Text("Ol"),
								),
							app.A().
								Class("permalink").
								Href("#Ol").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Ol()"),
						app.A().
							Href("#HTMLOl").
							Body(
								app.Text("HTMLOl"),
							),
					),
					app.P().Body(
						app.Text("Ol returns an HTML element that defines an ordered list."),
					),
					app.H2().
						ID("HTMLOptGroup").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=714123:719932#L28009").
								Body(
									app.Text("HTMLOptGroup"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLOptGroup").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLOptGroup is the interface that describes a <optgroup> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLOptGroup interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies that the specified element/group of elements should be disabled."),
							),
						app.Text("Disabled(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Label specifies a shorter label for the option."),
							),
						app.Text("Label(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("OptGroup").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=720031:720059#L28164").
								Body(
									app.Text("OptGroup"),
								),
							app.A().
								Class("permalink").
								Href("#OptGroup").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func OptGroup()"),
						app.A().
							Href("#HTMLOptGroup").
							Body(
								app.Text("HTMLOptGroup"),
							),
					),
					app.P().Body(
						app.Text("OptGroup returns an HTML element that defines a group of related options in a drop-down list."),
					),
					app.H2().
						ID("HTMLOption").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=725538:731437#L28434").
								Body(
									app.Text("HTMLOption"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLOption").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLOption is the interface that describes a <option> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLOption interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies that the specified element/group of elements should be disabled."),
							),
						app.Text("Disabled(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Label specifies a shorter label for the option."),
							),
						app.Text("Label(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Selected specifies that an option should be pre-selected when the page loads."),
							),
						app.Text("Selected(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Option").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=731517:731541#L28595").
								Body(
									app.Text("Option"),
								),
							app.A().
								Class("permalink").
								Href("#Option").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Option()"),
						app.A().
							Href("#HTMLOption").
							Body(
								app.Text("HTMLOption"),
							),
					),
					app.P().Body(
						app.Text("Option returns an HTML element that defines an option in a drop-down list."),
					),
					app.H2().
						ID("HTMLOutput").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=736995:742767#L28875").
								Body(
									app.Text("HTMLOutput"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLOutput").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLOutput is the interface that describes a <output> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLOutput interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// For specifies which form element(s) a label/calculation is bound to."),
							),
						app.Text("For(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Output").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=742810:742834#L29033").
								Body(
									app.Text("Output"),
								),
							app.A().
								Class("permalink").
								Href("#Output").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Output()"),
						app.A().
							Href("#HTMLOutput").
							Body(
								app.Text("HTMLOutput"),
							),
					),
					app.P().Body(
						app.Text("Output returns an HTML element that ."),
					),
					app.H2().
						ID("HTMLP").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=748171:753434#L29308").
								Body(
									app.Text("HTMLP"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLP").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLP is the interface that describes a <p> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLP interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("P").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=753491:753505#L29457").
								Body(
									app.Text("P"),
								),
							app.A().
								Class("permalink").
								Href("#P").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func P()"),
						app.A().
							Href("#HTMLP").
							Body(
								app.Text("HTMLP"),
							),
					),
					app.P().Body(
						app.Text("P returns an HTML element that defines a paragraph."),
					),
					app.H2().
						ID("HTMLParam").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=758105:763513#L29717").
								Body(
									app.Text("HTMLParam"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLParam").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLParam is the interface that describes a <param> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLParam interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Param").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=763588:763610#L29866").
								Body(
									app.Text("Param"),
								),
							app.A().
								Class("permalink").
								Href("#Param").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Param()"),
						app.A().
							Href("#HTMLParam").
							Body(
								app.Text("HTMLParam"),
							),
					),
					app.P().Body(
						app.Text("Param returns an HTML element that defines a parameter for an object."),
					),
					app.H2().
						ID("HTMLPicture").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=768617:774174#L30127").
								Body(
									app.Text("HTMLPicture"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLPicture").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLPicture is the interface that describes a <picture> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLPicture interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Picture").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=774266:774292#L30276").
								Body(
									app.Text("Picture"),
								),
							app.A().
								Class("permalink").
								Href("#Picture").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Picture()"),
						app.A().
							Href("#HTMLPicture").
							Body(
								app.Text("HTMLPicture"),
							),
					),
					app.P().Body(
						app.Text("Picture returns an HTML element that defines a container for multiple image resources."),
					),
					app.H2().
						ID("HTMLPre").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=779482:784843#L30536").
								Body(
									app.Text("HTMLPre"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLPre").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLPre is the interface that describes a <pre> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLPre interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Pre").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=784908:784926#L30685").
								Body(
									app.Text("Pre"),
								),
							app.A().
								Class("permalink").
								Href("#Pre").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Pre()"),
						app.A().
							Href("#HTMLPre").
							Body(
								app.Text("HTMLPre"),
							),
					),
					app.P().Body(
						app.Text("Pre returns an HTML element that defines preformatted text."),
					),
					app.H2().
						ID("HTMLProgress").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=789730:795484#L30945").
								Body(
									app.Text("HTMLProgress"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLProgress").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLProgress is the interface that describes a <progress> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLProgress interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Max Specifies the maximum value."),
							),
						app.Text("Max(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Value specifies the value of the element."),
							),
						app.Text("Value(v interface{})"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Progress").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=795562:795590#L31100").
								Body(
									app.Text("Progress"),
								),
							app.A().
								Class("permalink").
								Href("#Progress").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Progress()"),
						app.A().
							Href("#HTMLProgress").
							Body(
								app.Text("HTMLProgress"),
							),
					),
					app.P().Body(
						app.Text("Progress returns an HTML element that represents the progress of a task."),
					),
					app.H2().
						ID("HTMLQ").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=801056:806415#L31370").
								Body(
									app.Text("HTMLQ"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLQ").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLQ is the interface that describes a <q> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLQ interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Cite specifies a URL which explains the quote/deleted/inserted text."),
							),
						app.Text("Cite(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Q").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=806478:806492#L31522").
								Body(
									app.Text("Q"),
								),
							app.A().
								Class("permalink").
								Href("#Q").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Q()"),
						app.A().
							Href("#HTMLQ").
							Body(
								app.Text("HTMLQ"),
							),
					),
					app.P().Body(
						app.Text("Q returns an HTML element that defines a short quotation."),
					),
					app.H2().
						ID("HTMLRp").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=811176:816488#L31787").
								Body(
									app.Text("HTMLRp"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLRp").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLRp is the interface that describes a <rp> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLRp interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Rp").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=816596:816612#L31936").
								Body(
									app.Text("Rp"),
								),
							app.A().
								Class("permalink").
								Href("#Rp").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Rp()"),
						app.A().
							Href("#HTMLRp").
							Body(
								app.Text("HTMLRp"),
							),
					),
					app.P().Body(
						app.Text("Rp returns an HTML element that defines what to show in browsers that do not support ruby annotations."),
					),
					app.H2().
						ID("HTMLRt").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=821305:826617#L32196").
								Body(
									app.Text("HTMLRt"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLRt").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLRt is the interface that describes a <rt> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLRt interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Rt").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=826734:826750#L32345").
								Body(
									app.Text("Rt"),
								),
							app.A().
								Class("permalink").
								Href("#Rt").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Rt()"),
						app.A().
							Href("#HTMLRt").
							Body(
								app.Text("HTMLRt"),
							),
					),
					app.P().Body(
						app.Text("Rt returns an HTML element that defines an explanation/pronunciation of characters (for East Asian typography)."),
					),
					app.H2().
						ID("HTMLRuby").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=831447:836857#L32605").
								Body(
									app.Text("HTMLRuby"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLRuby").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLRuby is the interface that describes a <ruby> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLRuby interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Ruby").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=836951:836971#L32754").
								Body(
									app.Text("Ruby"),
								),
							app.A().
								Class("permalink").
								Href("#Ruby").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Ruby()"),
						app.A().
							Href("#HTMLRuby").
							Body(
								app.Text("HTMLRuby"),
							),
					),
					app.P().Body(
						app.Text("Ruby returns an HTML element that defines a ruby annotation (for East Asian typography)."),
					),
					app.H2().
						ID("HTMLS").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=841860:847123#L33014").
								Body(
									app.Text("HTMLS"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLS").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLS is the interface that describes a <s> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLS interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("S").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=847199:847213#L33163").
								Body(
									app.Text("S"),
								),
							app.A().
								Class("permalink").
								Href("#S").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func S()"),
						app.A().
							Href("#HTMLS").
							Body(
								app.Text("HTMLS"),
							),
					),
					app.P().Body(
						app.Text("S returns an HTML element that Defines text that is no longer correct."),
					),
					app.H2().
						ID("HTMLSamp").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=851811:857221#L33423").
								Body(
									app.Text("HTMLSamp"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSamp").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSamp is the interface that describes a <samp> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSamp interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Samp").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=857307:857327#L33572").
								Body(
									app.Text("Samp"),
								),
							app.A().
								Class("permalink").
								Href("#Samp").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Samp()"),
						app.A().
							Href("#HTMLSamp").
							Body(
								app.Text("HTMLSamp"),
							),
					),
					app.P().Body(
						app.Text("Samp returns an HTML element that defines sample output from a computer program."),
					),
					app.H2().
						ID("HTMLScript").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=862226:864545#L33832").
								Body(
									app.Text("HTMLScript"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLScript").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLScript is the interface that describes a <script> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLScript interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Async specifies that the script is executed asynchronously (only for external scripts)."),
							),
						app.Text("Async(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Charset specifies the character encoding."),
							),
						app.Text("Charset(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Defer specifies that the script is executed when the page has finished parsing (only for external scripts)."),
							),
						app.Text("Defer(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoad calls the given handler after the element is finished loading."),
							),
						app.Text("OnLoad(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Script").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=864616:864640#L33903").
								Body(
									app.Text("Script"),
								),
							app.A().
								Class("permalink").
								Href("#Script").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Script()"),
						app.A().
							Href("#HTMLScript").
							Body(
								app.Text("HTMLScript"),
							),
					),
					app.P().Body(
						app.Text("Script returns an HTML element that defines a client-side script."),
					),
					app.H2().
						ID("HTMLSection").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=866917:872474#L34033").
								Body(
									app.Text("HTMLSection"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSection").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSection is the interface that describes a <section> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSection interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Section").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=872549:872575#L34182").
								Body(
									app.Text("Section"),
								),
							app.A().
								Class("permalink").
								Href("#Section").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Section()"),
						app.A().
							Href("#HTMLSection").
							Body(
								app.Text("HTMLSection"),
							),
					),
					app.P().Body(
						app.Text("Section returns an HTML element that defines a section in a document."),
					),
					app.H2().
						ID("HTMLSelect").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=877771:883953#L34442").
								Body(
									app.Text("HTMLSelect"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSelect").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSelect is the interface that describes a <select> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSelect interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoFocus specifies that the element should automatically get focus when the page loads."),
							),
						app.Text("AutoFocus(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies that the specified element/group of elements should be disabled."),
							),
						app.Text("Disabled(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Multiple specifies that a user can enter more than one value."),
							),
						app.Text("Multiple(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Required specifies that the element must be filled out before submitting the form."),
							),
						app.Text("Required(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Size specifies the width."),
							),
						app.Text("Size(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Select").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=884020:884044#L34612").
								Body(
									app.Text("Select"),
								),
							app.A().
								Class("permalink").
								Href("#Select").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Select()"),
						app.A().
							Href("#HTMLSelect").
							Body(
								app.Text("HTMLSelect"),
							),
					),
					app.P().Body(
						app.Text("Select returns an HTML element that defines a drop-down list."),
					),
					app.H2().
						ID("HTMLSmall").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=889750:895209#L34907").
								Body(
									app.Text("HTMLSmall"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSmall").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSmall is the interface that describes a <small> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSmall interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Small").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=895271:895293#L35056").
								Body(
									app.Text("Small"),
								),
							app.A().
								Class("permalink").
								Href("#Small").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Small()"),
						app.A().
							Href("#HTMLSmall").
							Body(
								app.Text("HTMLSmall"),
							),
					),
					app.P().Body(
						app.Text("Small returns an HTML element that defines smaller text."),
					),
					app.H2().
						ID("HTMLSource").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=900291:906027#L35316").
								Body(
									app.Text("HTMLSource"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSource").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSource is the interface that describes a <source> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSource interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Media specifies what media/device the linked document is optimized for."),
							),
						app.Text("Media(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Sizes specifies the size of the linked resource."),
							),
						app.Text("Sizes(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// SrcSet specifies the URL of the image to use in different situations."),
							),
						app.Text("SrcSet(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Source").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=906070:906094#L35474").
								Body(
									app.Text("Source"),
								),
							app.A().
								Class("permalink").
								Href("#Source").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Source()"),
						app.A().
							Href("#HTMLSource").
							Body(
								app.Text("HTMLSource"),
							),
					),
					app.P().Body(
						app.Text("Source returns an HTML element that ."),
					),
					app.H2().
						ID("HTMLSpan").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=911477:916887#L35750").
								Body(
									app.Text("HTMLSpan"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSpan").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSpan is the interface that describes a <span> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSpan interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Span").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=916959:916979#L35899").
								Body(
									app.Text("Span"),
								),
							app.A().
								Class("permalink").
								Href("#Span").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Span()"),
						app.A().
							Href("#HTMLSpan").
							Body(
								app.Text("HTMLSpan"),
							),
					),
					app.P().Body(
						app.Text("Span returns an HTML element that defines a section in a document."),
					),
					app.H2().
						ID("HTMLStrong").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=921878:927386#L36159").
								Body(
									app.Text("HTMLStrong"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLStrong").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLStrong is the interface that describes a <strong> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLStrong interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Strong").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=927451:927475#L36308").
								Body(
									app.Text("Strong"),
								),
							app.A().
								Class("permalink").
								Href("#Strong").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Strong()"),
						app.A().
							Href("#HTMLStrong").
							Body(
								app.Text("HTMLStrong"),
							),
					),
					app.P().Body(
						app.Text("Strong returns an HTML element that defines important text."),
					),
					app.H2().
						ID("HTMLStyle").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=932570:938309#L36568").
								Body(
									app.Text("HTMLStyle"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLStyle").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLStyle is the interface that describes a <style> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLStyle interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Media specifies what media/device the linked document is optimized for."),
							),
						app.Text("Media(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type specifies the type of element."),
							),
						app.Text("Type(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoad calls the given handler after the element is finished loading."),
							),
						app.Text("OnLoad(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Style").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=938391:938413#L36726").
								Body(
									app.Text("Style"),
								),
							app.A().
								Class("permalink").
								Href("#Style").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Style()"),
						app.A().
							Href("#HTMLStyle").
							Body(
								app.Text("HTMLStyle"),
							),
					),
					app.P().Body(
						app.Text("Style returns an HTML element that defines style information for a document."),
					),
					app.H2().
						ID("HTMLSub").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=943669:949030#L37001").
								Body(
									app.Text("HTMLSub"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSub").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSub is the interface that describes a <sub> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSub interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Sub").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=949094:949112#L37150").
								Body(
									app.Text("Sub"),
								),
							app.A().
								Class("permalink").
								Href("#Sub").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Sub()"),
						app.A().
							Href("#HTMLSub").
							Body(
								app.Text("HTMLSub"),
							),
					),
					app.P().Body(
						app.Text("Sub returns an HTML element that defines subscripted text."),
					),
					app.H2().
						ID("HTMLSummary").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=953914:959471#L37410").
								Body(
									app.Text("HTMLSummary"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSummary").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSummary is the interface that describes a <summary> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSummary interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Summary").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=959562:959588#L37559").
								Body(
									app.Text("Summary"),
								),
							app.A().
								Class("permalink").
								Href("#Summary").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Summary()"),
						app.A().
							Href("#HTMLSummary").
							Body(
								app.Text("HTMLSummary"),
							),
					),
					app.P().Body(
						app.Text("Summary returns an HTML element that defines a visible heading for a details element."),
					),
					app.H2().
						ID("HTMLSup").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=964778:970139#L37819").
								Body(
									app.Text("HTMLSup"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLSup").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLSup is the interface that describes a <sup> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLSup interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Sup").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=970205:970223#L37968").
								Body(
									app.Text("Sup"),
								),
							app.A().
								Class("permalink").
								Href("#Sup").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Sup()"),
						app.A().
							Href("#HTMLSup").
							Body(
								app.Text("HTMLSup"),
							),
					),
					app.P().Body(
						app.Text("Sup returns an HTML element that defines superscripted text."),
					),
					app.H2().
						ID("HTMLTBody").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=985555:991014#L38637").
								Body(
									app.Text("HTMLTBody"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTBody").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTBody is the interface that describes a <tbody> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTBody interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("TBody").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=991090:991112#L38786").
								Body(
									app.Text("TBody"),
								),
							app.A().
								Class("permalink").
								Href("#TBody").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func TBody()"),
						app.A().
							Href("#HTMLTBody").
							Body(
								app.Text("HTMLTBody"),
							),
					),
					app.P().Body(
						app.Text("TBody returns an HTML element that groups the body content in a table."),
					),
					app.H2().
						ID("HTMLTHead").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1045311:1050770#L40995").
								Body(
									app.Text("HTMLTHead"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTHead").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTHead is the interface that describes a <thead> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTHead interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("THead").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1050847:1050869#L41144").
								Body(
									app.Text("THead"),
								),
							app.A().
								Class("permalink").
								Href("#THead").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func THead()"),
						app.A().
							Href("#HTMLTHead").
							Body(
								app.Text("HTMLTHead"),
							),
					),
					app.P().Body(
						app.Text("THead returns an HTML element that groups the header content in a table"),
					),
					app.H2().
						ID("HTMLTable").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=975021:980480#L38228").
								Body(
									app.Text("HTMLTable"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTable").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTable is the interface that describes a <table> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTable interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Table").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=980537:980559#L38377").
								Body(
									app.Text("Table"),
								),
							app.A().
								Class("permalink").
								Href("#Table").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Table()"),
						app.A().
							Href("#HTMLTable").
							Body(
								app.Text("HTMLTable"),
							),
					),
					app.P().Body(
						app.Text("Table returns an HTML element that defines a table."),
					),
					app.H2().
						ID("HTMLTd").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=996102:1001696#L39046").
								Body(
									app.Text("HTMLTd"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTd").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTd is the interface that describes a <td> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTd interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ColSpan specifies the number of columns a table cell should span."),
							),
						app.Text("ColSpan(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Headers specifies one or more headers cells a cell is related to."),
							),
						app.Text("Headers(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Rowspan specifies the number of rows a table cell should span."),
							),
						app.Text("Rowspan(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Td").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1001760:1001776#L39204").
								Body(
									app.Text("Td"),
								),
							app.A().
								Class("permalink").
								Href("#Td").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Td()"),
						app.A().
							Href("#HTMLTd").
							Body(
								app.Text("HTMLTd"),
							),
					),
					app.P().Body(
						app.Text("Td returns an HTML element that defines a cell in a table."),
					),
					app.H2().
						ID("HTMLTemplate").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1006721:1008489#L39479").
								Body(
									app.Text("HTMLTemplate"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTemplate").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTemplate is the interface that describes a <template> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTemplate interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Template").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1008552:1008580#L39532").
								Body(
									app.Text("Template"),
								),
							app.A().
								Class("permalink").
								Href("#Template").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Template()"),
						app.A().
							Href("#HTMLTemplate").
							Body(
								app.Text("HTMLTemplate"),
							),
					),
					app.P().Body(
						app.Text("Template returns an HTML element that defines a template."),
					),
					app.H2().
						ID("HTMLTextarea").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1010389:1017240#L39632").
								Body(
									app.Text("HTMLTextarea"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTextarea").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTextarea is the interface that describes a <textarea> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTextarea interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoFocus specifies that the element should automatically get focus when the page loads."),
							),
						app.Text("AutoFocus(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Cols specifies the visible width of a text area."),
							),
						app.Text("Cols(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DirName specifies that the text direction will be submitted."),
							),
						app.Text("DirName(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies that the specified element/group of elements should be disabled."),
							),
						app.Text("Disabled(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Form specifies the name of the form the element belongs to."),
							),
						app.Text("Form(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// MaxLength specifies the maximum number of characters allowed in an element."),
							),
						app.Text("MaxLength(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Name specifies the name of the element."),
							),
						app.Text("Name(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Placeholder specifies a short hint that describes the expected value of the element."),
							),
						app.Text("Placeholder(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ReadOnly specifies that the element is read-only."),
							),
						app.Text("ReadOnly(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Required specifies that the element must be filled out before submitting the form."),
							),
						app.Text("Required(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Rows specifies the visible number of lines in a text area."),
							),
						app.Text("Rows(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Wrap specifies how the text in a text area is to be wrapped when submitted in a form."),
							),
						app.Text("Wrap(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Textarea").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1017330:1017358#L39817").
								Body(
									app.Text("Textarea"),
								),
							app.A().
								Class("permalink").
								Href("#Textarea").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Textarea()"),
						app.A().
							Href("#HTMLTextarea").
							Body(
								app.Text("HTMLTextarea"),
							),
					),
					app.P().Body(
						app.Text("Textarea returns an HTML element that defines a multiline input control (text area)."),
					),
					app.H2().
						ID("HTMLTfoot").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1023754:1029213#L40137").
								Body(
									app.Text("HTMLTfoot"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTfoot").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTfoot is the interface that describes a <tfoot> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTfoot interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Tfoot").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1029291:1029313#L40286").
								Body(
									app.Text("Tfoot"),
								),
							app.A().
								Class("permalink").
								Href("#Tfoot").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Tfoot()"),
						app.A().
							Href("#HTMLTfoot").
							Body(
								app.Text("HTMLTfoot"),
							),
					),
					app.P().Body(
						app.Text("Tfoot returns an HTML element that groups the footer content in a table."),
					),
					app.H2().
						ID("HTMLTh").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1034303:1040127#L40546").
								Body(
									app.Text("HTMLTh"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTh").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTh is the interface that describes a <th> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTh interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Accept specifies an abbreviated version of the content in a header cell."),
							),
						app.Text("Accept(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ColSpan specifies the number of columns a table cell should span."),
							),
						app.Text("ColSpan(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Headers specifies one or more headers cells a cell is related to."),
							),
						app.Text("Headers(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Rowspan specifies the number of rows a table cell should span."),
							),
						app.Text("Rowspan(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Scope specifies whether a header cell is a header for a column, row, or group of columns or rows."),
							),
						app.Text("Scope(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Th").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1040198:1040214#L40710").
								Body(
									app.Text("Th"),
								),
							app.A().
								Class("permalink").
								Href("#Th").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Th()"),
						app.A().
							Href("#HTMLTh").
							Body(
								app.Text("HTMLTh"),
							),
					),
					app.P().Body(
						app.Text("Th returns an HTML element that defines a header cell in a table."),
					),
					app.H2().
						ID("HTMLTime").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1055863:1061345#L41404").
								Body(
									app.Text("HTMLTime"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTime").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTime is the interface that describes a <time> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTime interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DateTime specifies the date and time."),
							),
						app.Text("DateTime(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Time").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1061405:1061425#L41556").
								Body(
									app.Text("Time"),
								),
							app.A().
								Class("permalink").
								Href("#Time").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Time()"),
						app.A().
							Href("#HTMLTime").
							Body(
								app.Text("HTMLTime"),
							),
					),
					app.P().Body(
						app.Text("Time returns an HTML element that defines a date/time."),
					),
					app.H2().
						ID("HTMLTitle").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1066410:1068127#L41821").
								Body(
									app.Text("HTMLTitle"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTitle").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTitle is the interface that describes a <title> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTitle interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Title").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1068201:1068223#L41874").
								Body(
									app.Text("Title"),
								),
							app.A().
								Class("permalink").
								Href("#Title").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Title()"),
						app.A().
							Href("#HTMLTitle").
							Body(
								app.Text("HTMLTitle"),
							),
					),
					app.P().Body(
						app.Text("Title returns an HTML element that defines a title for the document."),
					),
					app.H2().
						ID("HTMLTr").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1069915:1075227#L41974").
								Body(
									app.Text("HTMLTr"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLTr").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLTr is the interface that describes a <tr> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLTr interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Tr").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1075290:1075306#L42123").
								Body(
									app.Text("Tr"),
								),
							app.A().
								Class("permalink").
								Href("#Tr").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Tr()"),
						app.A().
							Href("#HTMLTr").
							Body(
								app.Text("HTMLTr"),
							),
					),
					app.P().Body(
						app.Text("Tr returns an HTML element that defines a row in a table."),
					),
					app.H2().
						ID("HTMLU").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1079997:1085260#L42383").
								Body(
									app.Text("HTMLU"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLU").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLU is the interface that describes a <u> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLU interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("U").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1085366:1085380#L42532").
								Body(
									app.Text("U"),
								),
							app.A().
								Class("permalink").
								Href("#U").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func U()"),
						app.A().
							Href("#HTMLU").
							Body(
								app.Text("HTMLU"),
							),
					),
					app.P().Body(
						app.Text("U returns an HTML element that defines text that should be stylistically different from normal text."),
					),
					app.H2().
						ID("HTMLUl").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1089974:1095286#L42792").
								Body(
									app.Text("HTMLUl"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLUl").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLUl is the interface that describes a <ul> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLUl interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Ul").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1095350:1095366#L42941").
								Body(
									app.Text("Ul"),
								),
							app.A().
								Class("permalink").
								Href("#Ul").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Ul()"),
						app.A().
							Href("#HTMLUl").
							Body(
								app.Text("HTMLUl"),
							),
					),
					app.P().Body(
						app.Text("Ul returns an HTML element that defines an unordered list."),
					),
					app.H2().
						ID("HTMLVar").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1100061:1105422#L43201").
								Body(
									app.Text("HTMLVar"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLVar").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLVar is the interface that describes a <var> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLVar interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Var").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1105480:1105498#L43350").
								Body(
									app.Text("Var"),
								),
							app.A().
								Class("permalink").
								Href("#Var").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Var()"),
						app.A().
							Href("#HTMLVar").
							Body(
								app.Text("HTMLVar"),
							),
					),
					app.P().Body(
						app.Text("Var returns an HTML element that defines a variable."),
					),
					app.H2().
						ID("HTMLVideo").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1110296:1120005#L43610").
								Body(
									app.Text("HTMLVideo"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLVideo").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLVideo is the interface that describes a <video> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLVideo interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AutoPlay specifies that the audio/video will start playing as soon as it is ready."),
							),
						app.Text("AutoPlay(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Controls specifies that audio/video controls should be displayed (such as a play/pause button etc)."),
							),
						app.Text("Controls(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Height specifies the height of the element (in pixels)."),
							),
						app.Text("Height(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Loop specifies that the audio/video will start over again, every time it is finished."),
							),
						app.Text("Loop(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Muted specifies that the audio output of the video should be muted."),
							),
						app.Text("Muted(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Poster specifies an image to be shown while the video is downloading, or until the user hits the play button."),
							),
						app.Text("Poster(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Preload specifies if and how the author thinks the audio/video should be loaded when the page loads."),
							),
						app.Text("Preload(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Src specifies the URL of the media file."),
							),
						app.Text("Src(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Width specifies the width of the element."),
							),
						app.Text("Width(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnAbort calls the given handler on abort."),
							),
						app.Text("OnAbort(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlay calls the given handler when a file is ready to start playing (when it has buffered enough to begin)."),
							),
						app.Text("OnCanPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCanPlayThrough calls the given handler when a file can be played all the way to the end without pausing for buffering."),
							),
						app.Text("OnCanPlayThrough(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCueChange calls the given handler when the cue changes in a track element."),
							),
						app.Text("OnCueChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDurationChange calls the given handler when the length of the media changes."),
							),
						app.Text("OnDurationChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEmptied calls the given handler when something bad happens and the file is suddenly unavailable (like unexpectedly disconnects)."),
							),
						app.Text("OnEmptied(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnEnded calls the given handler when the media has reach the end."),
							),
						app.Text("OnEnded(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnError calls the given handler when an error occurs."),
							),
						app.Text("OnError(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadStart calls the given handler just as the file begins to load before anything is actually loaded."),
							),
						app.Text("OnLoadStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnLoadedData calls the given handler when media data is loaded."),
							),
						app.Text("OnLoadedData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPause calls the given handler when the media is paused either by the user or programmatically."),
							),
						app.Text("OnPause(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlay calls the given handler when the media is ready to start playing."),
							),
						app.Text("OnPlay(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPlaying calls the given handler when the media actually has started playing."),
							),
						app.Text("OnPlaying(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnProgress calls the given handler when the browser is in the process of getting the media data."),
							),
						app.Text("OnProgress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnRateChange calls the given handler each time the playback rate changes (like when a user switches to a slow motion or fast forward mode)."),
							),
						app.Text("OnRateChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeked calls the given handler when the seeking attribute is set to false indicating that seeking has ended."),
							),
						app.Text("OnSeeked(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSeeking calls the given handler when the seeking attribute is set to true indicating that seeking is active."),
							),
						app.Text("OnSeeking(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnStalled calls the given handler when the browser is unable to fetch the media data for whatever reason."),
							),
						app.Text("OnStalled(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSuspend calls the given handler when fetching the media data is stopped before it is completely loaded for whatever reason."),
							),
						app.Text("OnSuspend(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnTimeUpdate calls the given handler when the playing position has changed (like when the user fast forwards to a different point in the media)."),
							),
						app.Text("OnTimeUpdate(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnVolumeChange calls the given handler each time the volume is changed which (includes setting the volume to \"mute\")."),
							),
						app.Text("OnVolumeChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWaiting calls the given handler when the media has paused but is expected to resume (like when the media pauses to buffer more data)."),
							),
						app.Text("OnWaiting(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnloadedMetaData calls the given handler when meta data (like dimensions and duration) are loaded."),
							),
						app.Text("OnloadedMetaData(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Video").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1120071:1120093#L43855").
								Body(
									app.Text("Video"),
								),
							app.A().
								Class("permalink").
								Href("#Video").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Video()"),
						app.A().
							Href("#HTMLVideo").
							Body(
								app.Text("HTMLVideo"),
							),
					),
					app.P().Body(
						app.Text("Video returns an HTML element that defines a video or movie."),
					),
					app.H2().
						ID("HTMLWbr").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1128305:1133666#L44275").
								Body(
									app.Text("HTMLWbr"),
								),
							app.A().
								Class("permalink").
								Href("#HTMLWbr").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("HTMLWbr is the interface that describes a <wbr> HTML element."),
					),
					app.Pre().Body(
						app.Text("type HTMLWbr interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Body set the content of the element."),
							),
						app.Text("Body(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Text sets the content of the element with a text node containing the stringified given value."),
							),
						app.Text("Text(v interface{})"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AccessKey specifies a shortcut key to activate/focus an element."),
							),
						app.Text("AccessKey(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Aria stores accessible rich internet applications (ARIA) data."),
							),
						app.Text("Aria(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class specifies one or more classnames for an element (refers to a class in a style sheet). Multiple classnames are space separated."),
							),
						app.Text("Class(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ContentEditable specifies whether the content of an element is editable or not."),
							),
						app.Text("ContentEditable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DataSet stores custom data private to the page or application."),
							),
						app.Text("DataSet(k"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", v interface{})"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Dir specifies the text direction for the content in an element."),
							),
						app.Text("Dir(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Draggable specifies whether an element is draggable or not."),
							),
						app.Text("Draggable(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Hidden specifies that an element is not yet, or is no longer relevant."),
							),
						app.Text("Hidden(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ID specifies a unique id for an element."),
							),
						app.Text("ID(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Lang specifies the language of the element's content."),
							),
						app.Text("Lang(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Spellcheck specifies whether the element is to have its spelling and grammar checked or not."),
							),
						app.Text("Spellcheck(v"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Style specifies a CSS style for an element. Can be called multiple times to set multiple css styles."),
							),
						app.Text("Style(k, v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TabIndex specifies the tabbing order of an element."),
							),
						app.Text("TabIndex(v"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about an element."),
							),
						app.Text("Title(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnBlur calls the given handler when the element loses focus."),
							),
						app.Text("OnBlur(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnChange calls the given handler when the value of the element is changed."),
							),
						app.Text("OnChange(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnContextMenu calls the given handler when a context menu is triggered."),
							),
						app.Text("OnContextMenu(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCopy calls the given handler when the user copies the content of an element."),
							),
						app.Text("OnCopy(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnCut calls the given handler when the user cuts the content of an element."),
							),
						app.Text("OnCut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDblClick calls the given handler when there is a mouse double-click on the element."),
							),
						app.Text("OnDblClick(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrag calls the given handler when an element is dragged."),
							),
						app.Text("OnDrag(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnd calls the given handler at the end of a drag operation."),
							),
						app.Text("OnDragEnd(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragEnter calls the given handler when an element has been dragged to a valid drop target."),
							),
						app.Text("OnDragEnter(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragLeave calls the given handler when an element leaves a valid drop target."),
							),
						app.Text("OnDragLeave(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragOver calls the given handler when an element is being dragged over a valid drop target."),
							),
						app.Text("OnDragOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDragStart calls the given handler at the start of a drag operation."),
							),
						app.Text("OnDragStart(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnDrop calls the given handler when dragged element is being dropped."),
							),
						app.Text("OnDrop(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnFocus calls the given handler when the element gets focus."),
							),
						app.Text("OnFocus(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInput calls the given handler when an element gets user input."),
							),
						app.Text("OnInput(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnInvalid calls the given handler when an element is invalid."),
							),
						app.Text("OnInvalid(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyDown calls the given handler when a user is pressing a key."),
							),
						app.Text("OnKeyDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyPress calls the given handler when a user presses a key."),
							),
						app.Text("OnKeyPress(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnKeyup calls the given handler when a user releases a key."),
							),
						app.Text("OnKeyup(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseDown calls the given handler when a mouse button is pressed down on an element."),
							),
						app.Text("OnMouseDown(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseMove calls the given handler when the mouse pointer is moving while it is over an element."),
							),
						app.Text("OnMouseMove(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOut calls the given handler when the mouse pointer moves out of an element."),
							),
						app.Text("OnMouseOut(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseOver calls the given handler when the mouse pointer moves over an element."),
							),
						app.Text("OnMouseOver(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnMouseUp calls the given handler when a mouse button is released over an element."),
							),
						app.Text("OnMouseUp(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnPaste calls the given handler when the user pastes some content in an element."),
							),
						app.Text("OnPaste(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnReset calls the given handler when the Reset button in a form is clicked."),
							),
						app.Text("OnReset(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnScroll calls the given handler when an element's scrollbar is being scrolled."),
							),
						app.Text("OnScroll(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSearch calls the given handler when the user writes something in a search field."),
							),
						app.Text("OnSearch(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSelect calls the given handler after some text has been selected in an element."),
							),
						app.Text("OnSelect(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnSubmit calls the given handler when a form is submitted."),
							),
						app.Text("OnSubmit(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnWheel calls the given handler when the mouse wheel rolls up or down over an element."),
							),
						app.Text("OnWheel(h"),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Wbr").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/html.go?s=1133735:1133753#L44424").
								Body(
									app.Text("Wbr"),
								),
							app.A().
								Class("permalink").
								Href("#Wbr").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Wbr()"),
						app.A().
							Href("#HTMLWbr").
							Body(
								app.Text("HTMLWbr"),
							),
					),
					app.P().Body(
						app.Text("Wbr returns an HTML element that defines a possible line-break."),
					),
					app.H2().
						ID("Handler").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/http.go?s=390:3260#L19").
								Body(
									app.Text("Handler"),
								),
							app.A().
								Class("permalink").
								Href("#Handler").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Handler is an HTTP handler that serves an HTML page that loads a Go wasm app\nand its resources."),
					),
					app.Pre().Body(
						app.Text("type Handler struct {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The page authors."),
							),
						app.Span().
							ID("Handler.Author").
							Body(),
						app.Text("Author"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// A placeholder background color for the application page to display before"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// its stylesheets are loaded."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DEFAULT: #2d2c2c."),
							),
						app.Span().
							ID("Handler.BackgroundColor").
							Body(),
						app.Text("BackgroundColor"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The path of the static resources that the browser is caching in order to"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// provide offline mode."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Note that Icon, Styles and Scripts are already cached by default."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Paths are relative to the root directory."),
							),
						app.Span().
							ID("Handler.CacheableResources").
							Body(),
						app.Text("CacheableResources []"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The page description."),
							),
						app.Span().
							ID("Handler.Description").
							Body(),
						app.Text("Description"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The environment variables that are passed to the progressive web app."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Reserved keys:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// - GOAPP_VERSION"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// - GOAPP_GOAPP_STATIC_RESOURCES_URL"),
							),
						app.Span().
							ID("Handler.Env").
							Body(),
						app.Text("Env"),
						app.A().
							Href("#Environment").
							Body(
								app.Text("Environment"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The icon that is used for the PWA, favicon, loading and default not"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// found component."),
							),
						app.Span().
							ID("Handler.Icon").
							Body(),
						app.Text("Icon"),
						app.A().
							Href("#Icon").
							Body(
								app.Text("Icon"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The page keywords."),
							),
						app.Span().
							ID("Handler.Keywords").
							Body(),
						app.Text("Keywords []"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The text displayed while loading a page."),
							),
						app.Span().
							ID("Handler.LoadingLabel").
							Body(),
						app.Text("LoadingLabel"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The name of the web application as it is usually displayed to the user."),
							),
						app.Span().
							ID("Handler.Name").
							Body(),
						app.Text("Name"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Additional headers to be added in head element."),
							),
						app.Span().
							ID("Handler.RawHeaders").
							Body(),
						app.Text("RawHeaders []"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The paths or urls of the JavaScript files to use with the page."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// eg:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  app.Handler{"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//      Scripts: []string{"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//          \"/web/test.js\",            // Static resource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//          \"https://foo.com/test.js\", // External resource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//      },"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  },"),
							),
						app.Span().
							ID("Handler.Scripts").
							Body(),
						app.Text("Scripts []"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The name of the web application displayed to the user when there is not"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// enough space to display Name."),
							),
						app.Span().
							ID("Handler.ShortName").
							Body(),
						app.Text("ShortName"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The resource provider that provides static resources. Static resources"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// are always accessed from a path that starts with \"/web/\"."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// eg:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  \"/web/main.css\""),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Default: LocalDir(\"web\")"),
							),
						app.Span().
							ID("Handler.Resources").
							Body(),
						app.Text("Resources"),
						app.A().
							Href("#ResourceProvider").
							Body(
								app.Text("ResourceProvider"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The paths or urls of the CSS files to use with the page."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// eg:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  app.Handler{"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//      Styles: []string{"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//          \"/web/test.css\",            // Static resource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//          \"https://foo.com/test.css\", // External resource"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//      },"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  },"),
							),
						app.Span().
							ID("Handler.Styles").
							Body(),
						app.Text("Styles []"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The theme color for the application. This affects how the OS displays the"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// app (e.g., PWA title bar or Android's task switcher)."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DEFAULT: #2d2c2c."),
							),
						app.Span().
							ID("Handler.ThemeColor").
							Body(),
						app.Text("ThemeColor"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The page title."),
							),
						app.Span().
							ID("Handler.Title").
							Body(),
						app.Text("Title"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The version number. This is used in order to update the PWA application"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// in the browser. It must be set when deployed on a live system in order to"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// prevent recurring updates."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Default: Auto-generated in order to trigger pwa update on a local"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// development system."),
							),
						app.Span().
							ID("Handler.Version").
							Body(),
						app.Text("Version"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// contains filtered or unexported fields"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Handler.ServeHTTP").
						Body(
							app.Text("func (*Handler)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/http.go?s=9708:9775#L411").
								Body(
									app.Text("ServeHTTP"),
								),
							app.A().
								Class("permalink").
								Href("#Handler.ServeHTTP").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (h *"),
						app.A().
							Href("#Handler").
							Body(
								app.Text("Handler"),
							),
						app.Text(") ServeHTTP(w"),
						app.A().
							Href("/pkg/net/http/").
							Body(
								app.Text("http"),
							),
						app.Text("."),
						app.A().
							Href("/pkg/net/http/#ResponseWriter").
							Body(
								app.Text("ResponseWriter"),
							),
						app.Text(", r *"),
						app.A().
							Href("/pkg/net/http/").
							Body(
								app.Text("http"),
							),
						app.Text("."),
						app.A().
							Href("/pkg/net/http/#Request").
							Body(
								app.Text("Request"),
							),
						app.Text(")"),
					),
					app.H2().
						ID("Icon").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/http.go?s=14825:15362#L624").
								Body(
									app.Text("Icon"),
								),
							app.A().
								Class("permalink").
								Href("#Icon").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Icon describes a square image that is used in various places such as\napplication icon, favicon or loading icon."),
					),
					app.Pre().Body(
						app.Text("type Icon struct {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The path or url to a square image/png file. It must have a side of 192px."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Path is relative to the root directory."),
							),
						app.Span().
							ID("Icon.Default").
							Body(),
						app.Text("Default"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The path or url to larger square image/png file. It must have a side of"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// 512px."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Path is relative to the root directory."),
							),
						app.Span().
							ID("Icon.Large").
							Body(),
						app.Text("Large"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The path or url to a square image/png file that is used for IOS/IPadOS"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// home screen icon. It must have a side of 192px."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Path is relative to the root directory."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// DEFAULT: Icon.Default"),
							),
						app.Span().
							ID("Icon.AppleTouch").
							Body(),
						app.Text("AppleTouch"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text("}"),
					),
					app.H2().
						ID("Kind").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/node.go?s=776:790#L31").
								Body(
									app.Text("Kind"),
								),
							app.A().
								Class("permalink").
								Href("#Kind").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Kind represents the specific kind of a user interface element."),
					),
					app.Pre().Body(
						app.Text("type Kind"),
						app.A().
							Href("/pkg/builtin/#uint").
							Body(
								app.Text("uint"),
							),
					),
					app.Pre().Body(
						app.Text("const ("),
						app.Span().
							Class("comment").
							Body(
								app.Text("// UndefinedElem represents an undefined UI element."),
							),
						app.Span().
							ID("UndefinedElem").
							Body(
								app.Text("UndefinedElem"),
							),
						app.A().
							Href("#Kind").
							Body(
								app.Text("Kind"),
							),
						app.Text("="),
						app.A().
							Href("/pkg/builtin/#iota").
							Body(
								app.Text("iota"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// SimpleText represents a simple text element."),
							),
						app.Span().
							ID("SimpleText").
							Body(
								app.Text("SimpleText"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// HTML represents an HTML element."),
							),
						app.Span().
							ID("HTML").
							Body(
								app.Text("HTML"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Component represents a customized, independent and reusable UI element."),
							),
						app.Span().
							ID("Component").
							Body(
								app.Text("Component"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Selector represents an element that is used to select a subset of"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// elements within a given list."),
							),
						app.Span().
							ID("Selector").
							Body(
								app.Text("Selector"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// RawHTML represents an HTML element obtained from a raw HTML code snippet."),
							),
						app.Span().
							ID("RawHTML").
							Body(
								app.Text("RawHTML"),
							),
						app.Text(")"),
					),
					app.H3().
						ID("Kind.String").
						Body(
							app.Text("func (Kind)"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/node.go?s=792:821#L33").
								Body(
									app.Text("String"),
								),
							app.A().
								Class("permalink").
								Href("#Kind.String").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func (k"),
						app.A().
							Href("#Kind").
							Body(
								app.Text("Kind"),
							),
						app.Text(") String()"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
					),
					app.H2().
						ID("MenuItemNode").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/contextmenu.go?s=108:766#L1").
								Body(
									app.Text("MenuItemNode"),
								),
							app.A().
								Class("permalink").
								Href("#MenuItemNode").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("MenuItemNode is the interface that describes a menu node."),
					),
					app.Pre().Body(
						app.Text("type MenuItemNode interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Disabled specifies whether the menu item is disabled."),
							),
						app.Text("Disabled("),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Text(")"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Keys set the menu item keys."),
							),
						app.Text("Keys("),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Icon set the menu item icon."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// \"cmdorctrl\" is replaced by the platform corresponding key."),
							),
						app.Text("Icon("),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Label set the menu item label."),
							),
						app.Text("Label("),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OnClick calls the given handler when there is a mouse click on the element."),
							),
						app.Text("OnClick("),
						app.A().
							Href("#EventHandler").
							Body(
								app.Text("EventHandler"),
							),
						app.Text(")"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Separator specifies that the menu item is a separator."),
							),
						app.Text("Separator()"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Title specifies extra information about the item."),
							),
						app.Text("Title("),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("MenuItem").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/contextmenu.go?s=801:829#L26").
								Body(
									app.Text("MenuItem"),
								),
							app.A().
								Class("permalink").
								Href("#MenuItem").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func MenuItem()"),
						app.A().
							Href("#MenuItemNode").
							Body(
								app.Text("MenuItemNode"),
							),
					),
					app.P().Body(
						app.Text("MenuItem returns a menu item."),
					),
					app.H2().
						ID("Mounter").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=854:1017#L29").
								Body(
									app.Text("Mounter"),
								),
							app.A().
								Class("permalink").
								Href("#Mounter").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Mounter is the interface that describes a component that can perform\nadditional actions when mounted."),
					),
					app.Pre().Body(
						app.Text("type Mounter interface {"),
						app.A().
							Href("#Composer").
							Body(
								app.Text("Composer"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The function that is called when the component is mounted. It is always"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// called on the UI goroutine."),
							),
						app.Text("OnMount("),
						app.A().
							Href("#Context").
							Body(
								app.Text("Context"),
							),
						app.Text(")\n}"),
					),
					app.H2().
						ID("Navigator").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/component.go?s=1415:1593#L49").
								Body(
									app.Text("Navigator"),
								),
							app.A().
								Class("permalink").
								Href("#Navigator").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Navigator is the interface that describes a component that can perform\nadditional actions when navigated on."),
					),
					app.Pre().Body(
						app.Text("type Navigator interface {"),
						app.A().
							Href("#Composer").
							Body(
								app.Text("Composer"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The function that is called when the component is navigated on. It is"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// always called on the UI goroutine."),
							),
						app.Text("OnNav("),
						app.A().
							Href("#Context").
							Body(
								app.Text("Context"),
							),
						app.Text(", *"),
						app.A().
							Href("/pkg/net/url/").
							Body(
								app.Text("url"),
							),
						app.Text("."),
						app.A().
							Href("/pkg/net/url/#URL").
							Body(
								app.Text("URL"),
							),
						app.Text(")\n}"),
					),
					app.H2().
						ID("RangeLoop").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/range.go?s=216:714#L4").
								Body(
									app.Text("RangeLoop"),
								),
							app.A().
								Class("permalink").
								Href("#RangeLoop").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("RangeLoop represents a control structure that iterates within a slice, an\narray or a map."),
					),
					app.Pre().Body(
						app.Text("type RangeLoop interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Slice sets the loop content by repeating the given function for the"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// number of elements in the source."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// It panics if the range source is not a slice or an array."),
							),
						app.Text("Slice(f func("),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#RangeLoop").
							Body(
								app.Text("RangeLoop"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Map sets the loop content by repeating the given function for the number"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// of elements in the source. Elements are ordered by keys."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// It panics if the range source is not a map or if map keys are not strings."),
							),
						app.Text("Map(f func("),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#RangeLoop").
							Body(
								app.Text("RangeLoop"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Range").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/range.go?s=852:889#L22").
								Body(
									app.Text("Range"),
								),
							app.A().
								Class("permalink").
								Href("#Range").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Range(src interface{})"),
						app.A().
							Href("#RangeLoop").
							Body(
								app.Text("RangeLoop"),
							),
					),
					app.P().Body(
						app.Text("Range returns a range loop that iterates within the given source. Source must\nbe a slice, an array or a map with strings as keys."),
					),
					app.H2().
						ID("ResourceProvider").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/resource.go?s=833:1435#L18").
								Body(
									app.Text("ResourceProvider"),
								),
							app.A().
								Class("permalink").
								Href("#ResourceProvider").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("ResourceProvider is the interface that describes a provider for resources."),
					),
					app.P().Body(
						app.Text("App resources are the mandatory resources required to run a PWA. They are\ngenerated by the Handler and are accessible from the root path. Eg:"),
					),
					app.Pre().Body(
						app.Text("\"/app-worker.js\"\n\"/manifest.json\"\n\"/wasm_exec.js\""),
					),
					app.P().Body(
						app.Text("Static resources are the resources used by the PWA such as the web assembly\nbinary, styles, scripts, or images. They can be located on a localhost or a\nremote bucket. In order to avoid confusion with PWA required resources,\nstatic resources URL paths are always prefixed by \"/web\". Eg:"),
					),
					app.Pre().Body(
						app.Text("\"/web/app.wasm\"\n\"/web/main.css\"\n\"/web/background.jpg\""),
					),
					app.P().Body(
						app.Text("If the resource provider is an http.Handler, the handler is used to serve\nstatic resources requests."),
					),
					app.Pre().Body(
						app.Text("type ResourceProvider interface {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The path to the root directory where app resources are accessible."),
							),
						app.Text("AppResources()"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The path or URL where the web directory that contains static resources is"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// located."),
							),
						app.Text("StaticResources()"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The URL of the app.wasm file. This must match the pattern:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  StaticResources/web/WASM_FILE."),
							),
						app.Text("AppWASM()"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The URL of the robots.txt file. This must match the pattern:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  StaticResources/web/robots.txt."),
							),
						app.Text("RobotsTxt()"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The URL of the ads.txt file. This must match the pattern:"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//  StaticResources/web/ads.txt."),
							),
						app.Text("AdsTxt()"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("GitHubPages").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/resource.go?s=3012:3062#L111").
								Body(
									app.Text("GitHubPages"),
								),
							app.A().
								Class("permalink").
								Href("#GitHubPages").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func GitHubPages(repoName"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#ResourceProvider").
							Body(
								app.Text("ResourceProvider"),
							),
					),
					app.P().Body(
						app.Text("GitHubPages returns a resource provider that provides resources from GitHub\npages. This provider must only be used to generate static websites with the\nGenerateStaticWebsite function."),
					),
					app.H3().
						ID("LocalDir").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/resource.go?s=1559:1602#L41").
								Body(
									app.Text("LocalDir"),
								),
							app.A().
								Class("permalink").
								Href("#LocalDir").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func LocalDir(path"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#ResourceProvider").
							Body(
								app.Text("ResourceProvider"),
							),
					),
					app.P().Body(
						app.Text("LocalDir returns a resource provider that serves static resources from a\nlocal directory located at the given path."),
					),
					app.H3().
						ID("RemoteBucket").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/resource.go?s=2214:2260#L75").
								Body(
									app.Text("RemoteBucket"),
								),
							app.A().
								Class("permalink").
								Href("#RemoteBucket").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func RemoteBucket(url"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#ResourceProvider").
							Body(
								app.Text("ResourceProvider"),
							),
					),
					app.P().Body(
						app.Text("RemoteBucket returns a resource provider that provides resources from a\nremote bucket such as Amazon S3 or Google Cloud Storage."),
					),
					app.H2().
						ID("TestUIDescriptor").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/testing.go?s=207:921#L2").
								Body(
									app.Text("TestUIDescriptor"),
								),
							app.A().
								Class("permalink").
								Href("#TestUIDescriptor").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("TestUIDescriptor represents a descriptor that describes a UI element and its\nlocation from its parents."),
					),
					app.Pre().Body(
						app.Text("type TestUIDescriptor struct {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The location of the node. It is used by the TestMatch to find the"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// element to test."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// If empty, the expected UI element is compared with the root of the tree."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Otherwise, each integer represents the index of the element to traverse,"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// from the root's children to the element to compare"),
							),
						app.Span().
							ID("TestUIDescriptor.Path").
							Body(),
						app.Text("Path []"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// The element to compare with the element targeted by Path. Compare"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// behavior varies depending on the element kind."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Simple text elements only have their text value compared."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// HTML elements have their attribute compared and check if their event"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// handlers are set."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("//"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Components have their exported field values compared."),
							),
						app.Span().
							ID("TestUIDescriptor.Expected").
							Body(),
						app.Text("Expected"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text("}"),
					),
					app.H2().
						ID("Type").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=82:95#L1").
								Body(
									app.Text("Type"),
								),
							app.A().
								Class("permalink").
								Href("#Type").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Type represents the JavaScript type of a Value."),
					),
					app.Pre().Body(
						app.Text("type Type"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
					),
					app.P().Body(
						app.Text("Constants that enumerates the JavaScript types."),
					),
					app.Pre().Body(
						app.Text("const ("),
						app.Span().
							ID("TypeUndefined").
							Body(
								app.Text("TypeUndefined"),
							),
						app.A().
							Href("#Type").
							Body(
								app.Text("Type"),
							),
						app.Text("="),
						app.A().
							Href("/pkg/builtin/#iota").
							Body(
								app.Text("iota"),
							),
						app.Span().
							ID("TypeNull").
							Body(
								app.Text("TypeNull"),
							),
						app.Span().
							ID("TypeBoolean").
							Body(
								app.Text("TypeBoolean"),
							),
						app.Span().
							ID("TypeNumber").
							Body(
								app.Text("TypeNumber"),
							),
						app.Span().
							ID("TypeString").
							Body(
								app.Text("TypeString"),
							),
						app.Span().
							ID("TypeSymbol").
							Body(
								app.Text("TypeSymbol"),
							),
						app.Span().
							ID("TypeObject").
							Body(
								app.Text("TypeObject"),
							),
						app.Span().
							ID("TypeFunction").
							Body(
								app.Text("TypeFunction"),
							),
						app.Text(")"),
					),
					app.H2().
						ID("UI").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/node.go?s=229:708#L5").
								Body(
									app.Text("UI"),
								),
							app.A().
								Class("permalink").
								Href("#UI").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("UI is the interface that describes a user interface element such as\ncomponents and HTML elements."),
					),
					app.Pre().Body(
						app.Text("type UI interface {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Kind represents the specific kind of a UI element."),
							),
						app.Text("Kind()"),
						app.A().
							Href("#Kind").
							Body(
								app.Text("Kind"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JSValue returns the javascript value linked to the element."),
							),
						app.Text("JSValue()"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Reports whether the element is mounted."),
							),
						app.Text("Mounted()"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// contains filtered or unexported methods"),
							),
						app.Text("}"),
					),
					app.Pre().Body(
						app.Text("var ("),
						app.Span().
							Class("comment").
							Body(
								app.Text("// NotFound is the ui element that is displayed when a request is not"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// routed."),
							),
						app.Span().
							ID("NotFound").
							Body(
								app.Text("NotFound"),
							),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text("= &notFound{}\n)"),
					),
					app.H3().
						ID("FilterUIElems").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/node.go?s=1852:1886#L82").
								Body(
									app.Text("FilterUIElems"),
								),
							app.A().
								Class("permalink").
								Href("#FilterUIElems").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func FilterUIElems(uis ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(") []"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
					),
					app.P().Body(
						app.Text("FilterUIElems returns a filtered version of the given UI elements where\nselector elements such as If and Range are interpreted and removed. It also\nremove nil elements."),
					),
					app.P().Body(
						app.Text("It should be used only when implementing components that can accept content\nwith variadic arguments like HTML elements Body method."),
					),
					app.H3().
						ID("Raw").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/raw.go?s=322:343#L7").
								Body(
									app.Text("Raw"),
								),
							app.A().
								Class("permalink").
								Href("#Raw").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Raw(v"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
					),
					app.P().Body(
						app.Text("Raw returns a ui element from the given raw value. HTML raw value must have a\nsingle root."),
					),
					app.P().Body(
						app.Text("It is not recommended to use this kind of node since there is no check on the\nraw string content."),
					),
					app.H3().
						ID("Text").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/text.go?s=154:181#L3").
								Body(
									app.Text("Text"),
								),
							app.A().
								Class("permalink").
								Href("#Text").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Text(v interface{})"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
					),
					app.P().Body(
						app.Text("Text creates a simple text element."),
					),
					app.H2().
						ID("UIShell").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/shell.go?s=269:1180#L6").
								Body(
									app.Text("UIShell"),
								),
							app.A().
								Class("permalink").
								Href("#UIShell").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("UIShell is a component that responsively handles the disposition of a side\nmenu, a submenu, and a main content."),
					),
					app.Pre().Body(
						app.Text("type UIShell interface {"),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Class adds a CSS class to the layout."),
							),
						app.Text("Class(c"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Content sets the main content."),
							),
						app.Text("Content(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Menu sets the side menu."),
							),
						app.Text("Menu(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Submenu sets the second side menu."),
							),
						app.Text("Submenu(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// MenuButton sets the content of the button that displays an overlayed menu"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// when clicked. The button is displayed only when a menu is set and shrunk."),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Default is ☰."),
							),
						app.Text("MenuButton(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// OverlayMenu sets the content of the overlay menu. The overlay menu is"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// shown when the menu is shrunk and the menu button is clicked."),
							),
						app.Text("OverlayMenu(elems ..."),
						app.A().
							Href("#UI").
							Body(
								app.Text("UI"),
							),
						app.Text(")"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// ItemsBaseWidth set the base width for the menu, submenu and content in"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// px. Default is 300px."),
							),
						app.Text("ItemsBaseWidth(px"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// AlignItemsToCenter vertically aligns menus and content to center."),
							),
						app.Text("AlignItemsToCenter()"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Shell").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/shell.go?s=1298:1318#L40").
								Body(
									app.Text("Shell"),
								),
							app.A().
								Class("permalink").
								Href("#Shell").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Shell()"),
						app.A().
							Href("#UIShell").
							Body(
								app.Text("UIShell"),
							),
					),
					app.P().Body(
						app.Text("Shell creates a responsive layout that handles the disposition of a side\nmenu, a submenu, and a main content."),
					),
					app.H2().
						ID("Value").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=548:3524#L18").
								Body(
									app.Text("Value"),
								),
							app.A().
								Class("permalink").
								Href("#Value").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Value is the interface that represents a JavaScript value. On wasm\narchitecture, it wraps the Value from"),
						app.A().
							Href("https://golang.org/pkg/syscall/js/").
							Body(
								app.Text("https://golang.org/pkg/syscall/js/"),
							),
						app.Text("package."),
					),
					app.Pre().Body(
						app.Text("type Value interface {"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Bool returns the value v as a bool. It panics if v is not a JavaScript"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// boolean."),
							),
						app.Text("Bool()"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Call does a JavaScript call to the method m of value v with the given"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// arguments. It panics if v has no method m. The arguments get mapped to"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript values according to the ValueOf function."),
							),
						app.Text("Call(m"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", args ...interface{})"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Float returns the value v as a float64. It panics if v is not a"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript number."),
							),
						app.Text("Float()"),
						app.A().
							Href("/pkg/builtin/#float64").
							Body(
								app.Text("float64"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Get returns the JavaScript property p of value v. It panics if v is not a"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript object."),
							),
						app.Text("Get(p"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(")"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Index returns JavaScript index i of value v. It panics if v is not a"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript object."),
							),
						app.Text("Index(i"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(")"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// InstanceOf reports whether v is an instance of type t according to"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript's instanceof operator."),
							),
						app.Text("InstanceOf(t"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text(")"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Int returns the value v truncated to an int. It panics if v is not a"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript number."),
							),
						app.Text("Int()"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Invoke does a JavaScript call of the value v with the given arguments. It"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// panics if v is not a JavaScript function. The arguments get mapped to"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript values according to the ValueOf function."),
							),
						app.Text("Invoke(args ...interface{})"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// IsNaN reports whether v is the JavaScript value \"NaN\"."),
							),
						app.Text("IsNaN()"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// IsNull reports whether v is the JavaScript value \"null\"."),
							),
						app.Text("IsNull()"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// IsUndefined reports whether v is the JavaScript value \"undefined\"."),
							),
						app.Text("IsUndefined()"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JSValue implements Wrapper interface."),
							),
						app.Text("JSValue()"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Length returns the JavaScript property \"length\" of v. It panics if v is"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// not a JavaScript object."),
							),
						app.Text("Length()"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// New uses JavaScript's \"new\" operator with value v as constructor and the"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// given arguments. It panics if v is not a JavaScript function. The"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// arguments get mapped to JavaScript values according to the ValueOf"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// function."),
							),
						app.Text("New(args ...interface{})"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Set sets the JavaScript property p of value v to ValueOf(x). It panics if"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// v is not a JavaScript object."),
							),
						app.Text("Set(p"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Text(", x interface{})"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// SetIndex sets the JavaScript index i of value v to ValueOf(x). It panics"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// if v is not a JavaScript object."),
							),
						app.Text("SetIndex(i"),
						app.A().
							Href("/pkg/builtin/#int").
							Body(
								app.Text("int"),
							),
						app.Text(", x interface{})"),
						app.Span().
							Class("comment").
							Body(
								app.Text("// String returns the value v as a string. String is a special case because"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// of Go's String method convention. Unlike the other getters, it does not"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// panic if v's Type is not TypeString. Instead, it returns a string of the"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// form \"<T>\" or \"<T: V>\" where T is v's type and V is a string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// representation of v's value."),
							),
						app.Text("String()"),
						app.A().
							Href("/pkg/builtin/#string").
							Body(
								app.Text("string"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Truthy returns the JavaScript \"truthiness\" of the value v. In JavaScript,"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// false, 0, \"\", null, undefined, and NaN are \"falsy\", and everything else"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// is \"truthy\". See"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// https://developer.mozilla.org/en-US/docs/Glossary/Truthy."),
							),
						app.Text("Truthy()"),
						app.A().
							Href("/pkg/builtin/#bool").
							Body(
								app.Text("bool"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// Type returns the JavaScript type of the value v. It is similar to"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// JavaScript's typeof operator, except that it returns TypeNull instead of"),
							),
						app.Span().
							Class("comment").
							Body(
								app.Text("// TypeObject for null."),
							),
						app.Text("Type()"),
						app.A().
							Href("#Type").
							Body(
								app.Text("Type"),
							),
						app.Text("}"),
					),
					app.H3().
						ID("Null").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=3571:3588#L103").
								Body(
									app.Text("Null"),
								),
							app.A().
								Class("permalink").
								Href("#Null").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Null()"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
					),
					app.P().Body(
						app.Text("Null returns the JavaScript value \"null\"."),
					),
					app.H3().
						ID("Undefined").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=3664:3686#L108").
								Body(
									app.Text("Undefined"),
								),
							app.A().
								Class("permalink").
								Href("#Undefined").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func Undefined()"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
					),
					app.P().Body(
						app.Text("Undefined returns the JavaScript value \"undefined\"."),
					),
					app.H3().
						ID("ValueOf").
						Body(
							app.Text("func"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=4371:4404#L126").
								Body(
									app.Text("ValueOf"),
								),
							app.A().
								Class("permalink").
								Href("#ValueOf").
								Body(
									app.Text("¶"),
								),
						),
					app.Pre().Body(
						app.Text("func ValueOf(x interface{})"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
					),
					app.P().Body(
						app.Text("ValueOf returns x as a JavaScript value:"),
					),
					app.Pre().Body(
						app.Text("| Go                     | JavaScript             |\n| ---------------------- | ---------------------- |\n| js.Value               | [its value]            |\n| js.Func                | function               |\n| nil                    | null                   |\n| bool                   | boolean                |\n| integers and floats    | number                 |\n| string                 | string                 |\n| []interface{}          | new array              |\n| map[string]interface{} | new object             |"),
					),
					app.P().Body(
						app.Text("Panics if x is not one of the expected types."),
					),
					app.H2().
						ID("Wrapper").
						Body(
							app.Text("type"),
							app.A().
								Href("/src/github.com/maxence-charriere/go-app/v7/pkg/app/js.go?s=345:388#L11").
								Body(
									app.Text("Wrapper"),
								),
							app.A().
								Class("permalink").
								Href("#Wrapper").
								Body(
									app.Text("¶"),
								),
						),
					app.P().Body(
						app.Text("Wrapper is implemented by types that are backed by a JavaScript value."),
					),
					app.Pre().Body(
						app.Text("type Wrapper interface {\n    JSValue()"),
						app.A().
							Href("#Value").
							Body(
								app.Text("Value"),
							),
						app.Text("}"),
					),
					app.H2().
						ID("pkg-subdirectories").
						Body(
							app.Text("Subdirectories"),
						),
					app.Div().
						Class("pkg-dir").
						Body(
							app.Table().Body(
								app.Tr().Body(
									app.Th().
										Class("pkg-name").
										Body(
											app.Text("Name"),
										),
									app.Th().
										Class("pkg-synopsis").
										Body(
											app.Text("Synopsis"),
										),
								),
								app.Div().
									ID("footer").
									Body(
										app.Text("Build version go1.14.6."),
										app.Br(),
										app.Text("Except as"),
										app.A().
											Href("https://developers.google.com/site-policies#restrictions").
											Body(
												app.Text("noted"),
											),
										app.Text(",\nthe content of this page is licensed under the\nCreative Commons Attribution 3.0 License,\nand code is licensed under a"),
										app.A().
											Href("/LICENSE").
											Body(
												app.Text("BSD license"),
											),
										app.Text("."),
										app.Br(),
										app.A().
											Href("/doc/tos.html").
											Body(
												app.Text("Terms of Service"),
											),
										app.Text("|"),
										app.A().
											Href("http://www.google.com/intl/en/policies/privacy/").
											Body(
												app.Text("Privacy Policy"),
											),
									),
							),
						),
				),
		)
}
